# FCP AutoCut 버전 업데이트 로그

## beta v0.12.90 — 2026-08-01 (컴파운드 클립 자막)

### 새 기능
- **컴파운드 클립(Compound Clip) 자막 생성** (이슈 #8) — 타임라인에 컴파운드만 있으면
  "'클립 항목 분해(Break Apart Clip Items)' 후 다시 내보내 주세요"로 멈추던 것을,
  컴파운드 **내부 타임라인의 오디오를 직접 인식**해 자막을 만든다. 자막은 컴파운드
  클립 자체에 붙으므로 FCP에서 컴파운드를 옮겨도 같이 따라간다
  - 컴파운드를 **일부만 잘라 쓴 경우**(앞뒤 트리밍) 보이는 구간의 오디오만 분석
  - **컴파운드 안의 컴파운드**(중첩)도 인식
  - 커넥티드(연결)로 붙인 컴파운드도 동일하게 처리
- ⚠️ **무음컷은 컴파운드가 있으면 생략된다** (자막본은 정상 생성). 컴파운드 구간까지
  무음컷이 필요하면 기존대로 Break Apart 후 실행

### 수정
- 앵커 항목이 하나도 없는 클립에 자막을 붙일 때 대상이 잘못 잡히던 버그 — 컴파운드에서
  자막이 프로젝트가 아닌 컴파운드 원본 안쪽에 박히던 원인

## beta v0.12.89 — 2026-07-21 (편집기 가독성·프리뷰 자막 스타일)

### 개선
- **선택 바 고정** — 단어를 선택할 때만 나타나 본문 첫 줄을 덮던 액션 바(삭제·복원·
  여기부터 재생·고유명사 교정)를 상시 고정. 선택이 없으면 버튼만 비활성
- **긴 문단 자동 분리** — 구두점 없는 영어 STT가 한 덩어리 벽이 되던 것을, 32단어를
  넘는 문단은 발화 휴지(pause)가 큰 지점부터 재귀적으로 나눠 표시 (표시만 — 원문 불변)
- **프리뷰에 실제 자막 스타일 반영** — 영상 미리보기 자막이 고정 크기 알약이 아니라
  실제 출력과 같은 크기 배율(기준 1080/1920)·Y 위치·2줄 설정으로 표시. 사이드바에서
  크기·위치를 바꾸면 재생 화면에서 즉시 보인다

## beta v0.12.88 — 2026-07-20 (워드프로세서식 대본 편집)

### 새 기능
- **캐럿에서 바로 타이핑** — 더블클릭 편집 필드(뒤 단어를 덮던 오버레이) 삭제. 단어를
  클릭해 커서를 놓고 바로 고치면 그대로 반영된다(수정 단어는 파란색). ⌫도 글자 단위로
  동작하고, 단어의 마지막 글자까지 지우면 글자를 없애는 대신 취소선 삭제로 전환된다
- **⌘Z가 입력 묶음 단위** — 연속 타이핑(0.5초 이내)은 하나의 undo 단위로 병합
- 원문 불변 유지 — 공백·줄바꿈 입력(새 단어 생성)과 여러 단어에 걸친 변경은 거부된다.
  타임코드가 없는 단어는 만들 수 없기 때문. 단어 단위 삭제는 기존대로 선택 후 ⌫

### 동작 변경
- 커서만 있을 때 ⌫: 이전엔 '커서가 놓인 단어 전체 삭제' → 이제 '글자 하나 삭제'
  (워드프로세서 표준). 단어 삭제는 드래그/더블클릭 선택 후 ⌫ 또는 선택 바의 삭제 버튼

## beta v0.12.87 — 2026-07-20 (편집 진입 시 창 자동 확장)

### 버그 수정
- **편집 화면에서 왼쪽 사이드바가 잘리던 문제** — 창이 편집기 최소 폭보다 좁은 채
  편집 상태로 들어오면 사이드바가 화면 밖으로 밀렸다. 편집 진입 시 창을 필요 크기
  (1380×860, 화면 크기 내)로 자동 확장

## beta v0.12.86 — 2026-07-20 (단일 화면 개편 1차 — 편집기가 본 화면으로)

### 새 기능
- **편집기가 앱의 본 화면이 됐다** — 분석이 끝나면 별도 팝업 없이 그 자리가 대본
  편집기로 바뀐다. 화면 상태는 셋뿐: 드롭존 → 분석 진행 → 편집기(+하단 결과 바)
- **하단 결과 바** — 완료 화면을 대체. 내보내기가 화면을 바꾸지 않아 옵션을 바꿔
  재출력하는 반복이 끊기지 않는다
- **재분석 경고** — 언어·화자 분리 등 분석 옵션을 바꾼 채 실행하면 "편집이 사라집니다"
  확인 대화상자. 편집이 없으면 묻지 않는다
- **자막 스타일이 사이드바로** — 크기·길이·2줄을 시트 없이 사이드바에서 바로 조절.
  위치·안전영역 가이드는 '위치·가이드…'로 축소된 시트에 유지
- 출력 옵션은 분석 전 비활성(흐림) — 결정할 근거가 생긴 뒤에 결정하도록. '1단계/2단계'
  라벨 제거

### 내부
- 편집기 파일 분해: 1746줄 → 본체 695줄 + PlayerPane·AgentPanel·CaptionCardsView·모델
- 편집 상태를 뷰에서 분리(TranscriptEditor) — undo/redo·경계 재계산 의미를 테스트 9개로 고정
- 화면 상태 열거형(MainStage) + 상태별 옵션 활성 규칙 테스트

## beta v0.12.85 — 2026-07-20 (대본 편집기 삭제 속도)

### 개선
- **대본 편집기에서 단어 삭제가 빨라졌다** — 단어 하나를 지울 때마다 대본 전체를
  다시 그리던 것을, 바뀐 단어의 표시 속성만 고치도록 변경. 2,000단어 문서 기준
  **31.9ms → 0.17ms (약 186배)**. 기존에는 삭제 한 번이 60fps 기준 약 2프레임을
  잡아먹어 연속 삭제가 밀렸다.
  - 문자열 길이를 바꾸는 변경(단어 수정·섹션 헤딩·읽기 모드)은 종전대로 전체 재구성.
    삭제·군소리 표시는 취소선/색만 바뀌어 길이가 그대로이므로 부분 갱신 대상.

## beta v0.12.84 — 2026-07-20 (영어 자막 누락 · 자막 겹침 수정)

> v0.12.83은 배포 전에 내용이 바뀌어 이 버전으로 합쳤다.

### 버그 수정
- **영어 자막이 대부분 사라지던 문제** — 환청 필터가 긴 실제 발화를 퇴화 출력으로
  오판했다. 영어 30초 발화는 관사·전치사가 반복되므로 '한 토큰 3회 이상 반복' 규칙에
  전부 걸렸다(24개 구간 중 22개 삭제). 고유단어 비율로만 판정하도록 수정 —
  같은 영상에서 자막 16개 → 560개, 구간 커버리지 107~357초 → 0~571초
- **대본 편집기에서 자막 카드가 겹쳐 보이던 문제** — 카드 경계 재계산 요청이 이미
  실행 중이면 재시도 없이 폐기돼, 병합(⌫)·분리(⌘Enter)를 연타하면 마지막 편집이
  반영되지 않고 카드 경계가 실제 텍스트와 어긋났다. 요청을 합치되 마지막 요청은
  반드시 실행하도록 수정
- **단어 수정 필드가 뒤 단어를 덮던 문제** — 인라인 편집 필드 폭이 단어보다 24pt
  넓어 다음 단어를 가렸다. 단어 폭에 맞춤

### 개선
- 사이드바 하단을 세 줄로 분리 — 좁은 폭에서 `beta v0…`, `Impr…` 처럼 잘리던 것을
  해소(버전·배지 / 잔여 횟수 / 피드백)
- 자막 스타일 아이콘이 한국어 시스템에서 `가가`로 표시되던 문제 — 로케일 가변
  SF Symbol이 시스템 언어를 따르던 것을 앱 UI 언어 기준으로 변경
- 영어 UI의 `Final Cut` 표기를 `Final Cut Pro`로 통일 (15군데)
- 결과 파일 행에 호버 피드백(배경 강조 + 포인터 커서) — 더블클릭·드래그가 가능하다는
  것이 시각적으로 드러나지 않던 문제
- 작업 완료 후 GitHub 별 요청 (한 번 누르거나 닫으면 다시 표시하지 않음, 베타 전용)

## beta v0.12.82 — 2026-07-18 (자막 카드 편집 · 파이널컷 타이틀 · 스토리라인 · 이중언어)

### 새 기능
- **자막 카드 편집(CapCut식)** — 대본 편집기 「자막 보기」: 실제 출력과 동일한 자막 카드를
  번호순으로 보고 클릭 편집·**병합(⌫)·분리(⌘Enter)**·방향키 카드 이동. 길이·2줄을 툴바에서
  즉시 조절(카드 수 실시간 반영), 결과는 내보내기까지 반영
- **파이널컷 타이틀 지정** — 타임라인의 타이틀 클립을 드래그하거나 「고르기」 목록(FCP Titles
  브라우저와 동일 소스, 검색·카테고리)에서 선택하면 **그 타이틀 룩으로 자막 생성**
- **스토리라인으로 묶기** — 자막을 Create Storyline(보조 스토리라인)으로 그룹화. 화자 분리
  시 화자별 분리. 자막본·무음제거본 모두 지원
- **고정밀 화자분리(pyannote)** — 겹치는 발화 감지. 설정에 HuggingFace 토큰 등록 시 활성,
  기본 방식도 창 세밀화·라벨 스무딩으로 개선
- **구두점 바이블** — LLM 임의 삽입 폐지: `?`는 질문에만(문법 판정, ‘-세요’ 의문 지원),
  쉼표는 **실제 발화 쉼** 위치만. 음악/비발화 구간 **환청 자막 차단**(발화 게이트)
- **영어 UI + 한국어 완전 현지화** — 설정 › 일반에서 언어 전환(시스템/한국어/English)
- **MAS(앱스토어) 출시 준비** — CutPilot 빌드 파이프라인(샌드박스 서명·pkg), 자동(다국어)
  모드 지배 언어 고정(영어 영상 부분 번역 방지)

### 수정
- FCP 임포트 "not on an edit frame boundary" 근본 해결(타이틀 델타 격자)
- 화자 2명인데 타이틀 색 3개로 나오던 문제(미배정 자막 → 최근접 화자)
- UI/UX 정리 — 사이드바 계층화(고급 옵션 접힘)·색상 체계 3군 정리
- 앱 용량 축소 — 미사용 번들 라이브러리 정리로 DMG 약 63MB 감소

## beta v0.12.81 — 2026-07-16 (대본 편집기 전면 재작성 · AI 에이전트 · 예상 시간)

### 새 기능
- **대본 편집기 — 텍스트 기반 재작성** (분석 후 "대본 편집", **별도 창**):
  - **문서처럼 편집** — 네이티브 텍스트 뷰에서 드래그로 범위 선택, 클릭으로 그 위치 재생,
    **더블클릭으로 단어를 그 자리에서 바로 수정**. 문장 단위 문단, **가독성 뷰·전체보기** 전환
  - **지우면 타임라인이 잘림** — 삭제 구간은 자막은 그대로 두고 ‘_편집’ 결과물에서 컷(비파괴),
    재생 시 **삭제 구간 자동 스킵**
  - **한 번에 정리** — 무음컷·군소리 삭제(강도 반영) 일괄 버튼
  - **프리뷰 + 파형** — 비디오(자막 오버레이는 실제 자막 길이)·파형 타임라인(클릭 시 텍스트도 따라 스크롤)
  - **고유명사 즉시 교정** — 단어를 골라 올바른 표기 입력 → **전사 전체의 같은 단어를 즉시 치환**(조사 보존)·용어사전 등록
- **AI 에이전트** (대본 편집기 좌측 · 로컬 AI · **컷 전용**) — 원문은 그대로 두고 필요한 부분만 삭제, 검토·복원 가능:
  - **요약** — "핵심만 / 5분으로 / 1분으로", 또는 자유 요청. 문장 중요도로 목표 길이에 **핵심부터** 채움
  - **Google SEO 구조 정리** — **H2·H3 목차** 생성, 목차에서 **섹션 전체 삭제**
  - **구두점 넣기** — 쉼표·마침표·물음표·느낌표·따옴표·괄호 중 **원하는 부호만 체크**해 추가
  - **모델** — 기본 **번들 로컬(오프라인)**, 옵션으로 OpenAI·Gemini·Claude API 키
- **군소리 컷 약/중/강** — 강도 선택 (약: 순수 추임새 고립 / 중: +문장 속 낀 추임새 / 강: +"그/뭐/좀" 담화표지)
- **화자 수 수동 고정** — 화자 분리 시 자동/2·3·4명으로 인원을 고정해 **과분리(3~4명 튐) 방지**
- **예상 분석 시간** — 드롭 즉시 "예상 ~X분" 표시, 실행 중 갱신·매 실행 실측 보정
- **File ▸ SRT / TXT 대본 내보내기** — 완성된 자막본에서 즉시 추출해 다운로드 폴더로

### 개선
- 대본 편집기를 **별도 팝업 창**으로 분리(ESC로 안 닫힘, X로만 닫힘) + 선택·렌더 **성능·안정성** 대폭 향상
- 2단계 워크플로(v0.12.78~80 누적): 분석 1회 후 옵션·대본 편집을 몇 초 재출력으로

### 제거
- 편집기의 AI 챕터 헤더(엔진 `--chapters`) — **Google SEO 구조 정리(H2/H3)**로 대체

## beta v0.12.78 이전 항목은 아래 참조

## beta v0.12.78 — 2026-07-14 (2단계 워크플로 — 분석 1회 · 옵션은 몇 초 재출력)

### 새 기능
- **2단계 워크플로.** 사이드바가 "1단계 · 음성 분석"(언어·화자 분리·고유명사 교정)과
  "2단계 · 자막 · 출력"(자막 형식·스타일·Subtitles 롤·빈틈없이·교체 대비 여유·무음컷·
  군소리 컷)으로 나뉩니다. 작업 완료 후 2단계 옵션을 바꾸고 **"다시 내보내기"** 를
  누르면 음성 인식 없이 **몇 초 만에** 새 fcpxmld가 나옵니다 (분석 캐시 재사용,
  무료 사용 횟수 미차감). 1단계 옵션을 바꾸면 버튼이 "다시 분석"으로 바뀝니다.
- **File ▸ SRT / TXT 대본 내보내기** — 완성된 자막본에서 즉시 추출해 다운로드 폴더에
  저장합니다 (사이드바 토글 대신 메뉴로 이동, 재작업 불필요).
- **무음컷·군소리 컷도 2단계로** — 단어 타임스탬프 캐시로 STT 재실행 없이 켜고 끕니다.

### 개선
- 캐시 재사용 실행은 진행 화면에서 오디오 추출·음성 구간·음성 인식 단계가 생략된
  짧은 목록으로 표시됩니다.

## beta v0.12.76 — 2026-07-14 (혼합 프레임 레이트 임포트 오류 해결 · 교체 대비 여유)

### 버그 수정
- **30p 프로젝트 + 29.97 소스(혼합 레이트)에서 모든 자막이 "not on an edit frame
  boundary"로 거부되던 문제 해결.** 자막 duration을 시퀀스 프레임 격자로 확정
  (offset은 미디어 격자 유지 — FCP 임포트 검증 실측 규칙). 같은 레이트 프로젝트는
  기존과 동일하게 정확히 맞닿습니다.
- **같은 이름 프로젝트 중첩으로 옛 결과물을 재작업하는 혼동 차단** — 출력 프로젝트
  이름에 항상 고유 시각이 붙습니다(`이름_자막 1613` 형식, 기존 `_자막_자막` 중첩
  접미사는 자동 정리 · NFD 한글 이름 대응). 드롭 즉시 입력 요약(프로젝트명·클립 수)이
  표시되고, 앱이 만든 이전 결과물을 다시 드롭하면 ⚠️ 경고가 뜹니다.

### 새 기능
- **교체 대비 여유(옵션)** — 켜면 자막 끝을 1프레임 줄여, 파이널컷에서 타이틀을 다른
  템플릿으로 교체해도 자막이 윗줄(레인)로 튀지 않습니다. 혼합 레이트 프로젝트에서
  교체 작업을 할 때 권장 (기본 꺼짐 — 빈틈없이 채우기 유지).

### 개선
- **8개 상용 프레임 레이트(23.98~60p) 정합 자동 검증** — 임포트한 프로젝트와 정확히
  같은 격자로 자막이 생성됨을 회귀 테스트로 보장합니다.
- **파이널컷 네이티브 패리티** — 생성되는 Subtitle 타이틀이 파이널컷이 내부에서 만드는
  것과 동일한 XML 구조(퍼블리시 파라미터 20종 포함)를 갖습니다.

## beta v0.12.71 — 2026-07-12 (Background Tasks 스타일 진행 화면 · 더블클릭 가져오기)

### 개선
- **작업 진행 화면을 파이널컷 Background Tasks 스타일로** — 타임라인 분석 → 오디오 추출 →
  음성 구간 감지 → 음성 인식·정렬(%) → 자막 생성 → (무음컷) → 검증·저장 단계가
  행 목록으로 표시되고, 각 행 오른쪽에 대기/진행률/완료 체크가 실시간 갱신됩니다.
- **결과물 더블클릭 = 파이널컷 가져오기** — 완료 카드의 .fcpxmld를 더블클릭하면
  드래그앤드롭과 동일하게 파이널컷의 라이브러리 선택(가져오기) 창이 바로 뜹니다.
- **자막 스타일 프리뷰 개선** — 현재 macOS 배경화면(라이트/다크 반영, 은은한 블러)이
  프리뷰 배경으로 깔리고, 파이널컷 기본 세이프존(Action Safe 90%) 외곽선이 표시되며
  자막 드래그가 1:1로 따라오다 그 선에 정확히 닿으면 멈춥니다.

## beta v0.12.70 — 2026-07-12 (피드백에 작업물·로그 자동 동봉)

### 개선
- **'피드백 보내기' 한 번으로 자료까지 준비** — 에러가 없어도, 작업 로그 전체와
  **작업된 .fcpxmld 결과물**을 zip으로 묶어 Finder에 띄우고, 환경정보·로그 요약이
  채워진 GitHub 이슈 화면을 엽니다. zip을 이슈에 드래그하면 첨부 끝.
- 피드백 버튼이 반응하지 않던 문제 수정(한글 로그의 URL 인코딩 팽창) + 실패 시에도
  항상 이슈 페이지가 열리도록 폴백.

## beta v0.12.69 — 2026-07-12 (쇼츠 글자 크기 = 가로와 동일 · 피드백 GitHub 연동)

### 버그 수정
- **9:16(쇼츠) 자막이 가로보다 작게 렌더되던 문제 해결.** 템플릿이 세로에선 폭 기준으로
  축소되는 것을 보정해, 같은 pt가 가로·세로에서 **화면 대비 같은 크기**로 보입니다.
  한 줄 글자수 제한도 세로의 실제 렌더 폭 기준으로 계산됩니다(소프트 초과까지 하드 보장).

### 새 기능
- **피드백 → GitHub 이슈 자동 등록.** '피드백 보내기'를 누르면 앱 버전·macOS·FCP 버전과
  **최근 작업 로그가 자동 첨부**된 이슈 작성 화면이 열립니다 — 제출만 누르면 등록.

### 보안
- 배포 DMG의 엔진을 **바이트코드(.pyc)로만 포함** — 소스 평문 노출 차단.

## beta v0.12.67 — 2026-07-12 (원클릭 완전 자동 업데이트)

### 개선
- **"지금 업데이트" 한 번이면 끝** — 최신 버전을 내려받아 검증한 뒤, **앱을 스스로 종료하고
  새 버전으로 교체·재실행까지 자동**으로 진행합니다 (드래그 설치 불필요). 업데이트는 항상
  가장 최신 릴리스로 한 번에 이동합니다. 실패 시엔 기존 방식(설치 창)으로 폴백.
- **배포 파이프라인: Developer ID + 공증(notarization) 준비** — 인증서가 설정되면 DMG가
  자동으로 hardened runtime 서명·공증되어, **새로 내려받은 앱도 보안 경고 없이** 열립니다
  (베타테스터 피드백 반영 — 활성화 전까지는 기존과 동일).

## beta v0.12.66 — 2026-07-12 (사이드바 새 디자인 · 알림 설정 · 메뉴 단순화)

### 개선
- **사이드바 새 디자인** — macOS 시스템 설정 스타일: 옵션마다 컬러 아이콘 타일,
  섹션은 라운드 그룹 카드로 표시됩니다.
- **알림 설정** — 설정 > 일반에서 **작업 완료 알림**과 **업데이트 알림**을 각각 켜고
  끌 수 있습니다. 업데이트 알림은 앱 실행 중에도 1시간마다 확인해 새 버전이 나오면
  즉시 배너로 알려드립니다(같은 버전은 한 번만).
- **타이틀 메뉴 제거(단순화)** — 자막은 기본 Custom 타이틀로 생성되고,
  **'Subtitles 롤'을 켜면** 파이널컷 12.3 자막(Subtitle) 타이틀 + Subtitles 롤로
  전환됩니다. 별도 스타일 선택이 필요 없습니다.

## beta v0.12.65 — 2026-07-12 (메뉴 이름 정리 — '고유명사 교정')

### 변경
- **"용어 사전 치환" → "고유명사 교정"** — 사이드바 토글·설정 탭·검토 창 이름을
  기능이 하는 일 그대로 바꿨습니다. 설정 톱니 버튼은 제목 바로 옆으로 이동.

## beta v0.12.64 — 2026-07-12 (문장 종결 인식 보강 — 실산출 검수 기반)

### 개선
- **문장 끝 인식 사전 보강.** 실제 산출물 205개 카드를 전수 검수한 결과, 문장 경계
  규칙 자체는 완벽히 작동했지만(위반 0건) **종결어미 사전이 놓치는 패턴 3종**이
  24건의 "한 줄에 두 문장" 사례를 만들고 있었습니다 — `~세요`(안녕하세요),
  `~고요`(물어보고요), `~게요`(정리할게요)를 종결로 인식하도록 추가했습니다.
  같은 영상 기준 Bible 매칭률 87% → 100% (검수 기준).
- 재시작 선언("다시 읽을게요")이 종결로 인식되며 재읽기 컷을 막던 부작용도
  함께 보정했습니다.

## beta v0.12.63 — 2026-07-12 (분석 속도 복원 — 병렬 예열의 안전판)

### 개선
- **모델 로드가 다시 임계경로에서 빠집니다.** 한때 백그라운드 모델 예열로 빨라졌다가
  MLX 크래시("no Stream(gpu) in current thread")로 되돌려 느려졌던 것을, 방향을 뒤집어
  안전하게 복원했습니다 — 모델 로드(MLX)는 메인 스레드에 두고, **오디오 추출(ffmpeg)과
  음성 구간 감지(VAD)를 백그라운드**로 보내 둘을 겹칩니다. 모델 로드 시간(수십 초)이
  전체 소요에서 사실상 사라집니다. (로그에 "[시간] 모델 로드 …s (추출·VAD와 병렬)" 표시)

## beta v0.12.62 — 2026-07-12 (설정 유지 · 깔끔한 기본값)

### 개선
- **모든 옵션이 앱을 종료해도 유지됩니다.** 자막 자동화·언어·자막 형식·타이틀 스타일·
  Subtitles 롤·빈틈없이 채우기·무음컷·컷 강도·군소리 컷·화자 분리·용어 사전 치환 —
  마지막 설정 그대로 다시 시작합니다.
- **기본 세팅을 단순화** — 처음 시작하면 **'자막 자동화'만 켜져** 있습니다. 나머지
  (무음컷·군소리 컷·화자 분리·용어 사전 치환)는 필요할 때 켜세요.

## beta v0.12.61 — 2026-07-12 (frame boundary 완전 해결 · 문장 경계 규칙 · 속도 계측)

### 버그 수정
- **duration의 "edit frame boundary" 경고 해결.** 파이널컷 규칙 실측: 클립에 앵커된
  자막의 **offset은 미디어 프레임 격자, duration은 시퀀스(편집) 격자**를 따라야 합니다.
  30p 프로젝트 + 29.97 소스에서 offset은 v0.12.58로, duration은 이번 수정으로 정렬됩니다.

### 개선
- **문장 경계 하드 규칙.** 자막 길이 설정과 무관하게, 한 자막(한 줄)에 **앞 문장의 끝과
  다음 문장의 시작이 함께 담기지 않습니다** — 종결어미에서 카드가 반드시 끝납니다.
- **속도 개선·계측.** 같은 미디어 파일을 쓰는 클립이 여러 개여도 음성 구간 감지(VAD)를
  1회만 수행합니다(연결 클립 다수 프로젝트에서 크게 단축). 로그에 단계별 소요 시간
  (`[시간] 오디오 추출/VAD/ASR+정렬/분석 합계/총 소요`)이 표시돼, 어느 단계가 느린지
  바로 확인할 수 있습니다.

## beta v0.12.60 — 2026-07-12 (프리뷰 위치 = 파이널컷 위치, 항상)

### 개선
- **자막 위치를 조절하지 않았을 때도 프리뷰 위치가 그대로 파이널컷에 적용됩니다.**
  기존에는 미조절 시 파이널컷 템플릿의 자체 기본 위치로 렌더돼, 프리뷰에 보이는
  기본 위치(가로 -420 / 세로 -360)와 어긋날 수 있었습니다. 이제 항상 프리뷰 값을
  전달해 **조절 여부와 무관하게 프리뷰 = 파이널컷**입니다.

## beta v0.12.59 — 2026-07-12 (의도치 않은 2줄 자막 방지)

### 버그 수정
- **2줄을 켜지 않았는데 자막이 2줄로 나오던 문제 해결.** 자막 카드가 화면 폭보다 길면
  파이널컷 텍스트 박스가 임의로 줄바꿈해 생기던 현상입니다. 이제 선택한 글자 크기로
  **한 줄에 들어가는 글자수를 자동 계산**해 카드 길이를 그 이하로 제한합니다
  (로그에 "한 줄 폭 제한" 안내 표시 — 더 길게 담으려면 2줄을 켜세요).

## beta v0.12.58 — 2026-07-12 (edit frame boundary 경고 해결 — 30p 프로젝트)

### 버그 수정
- **"The item is not on an edit frame boundary" 경고 수백 개가 뜨던 문제 해결 (이슈 #4).**
  30p 프로젝트에 29.97fps 소스처럼 시퀀스와 미디어의 프레임레이트가 다르면, 자막이
  시퀀스 격자로 스냅돼 경고가 발생했습니다. 이제 클립에 붙는 자막은 **그 미디어의
  프레임 격자**에 정확히 정렬됩니다 (같은 레이트일 때는 기존과 동일).

## beta v0.12.57 — 2026-07-12 (빈틈없이 채우기 — 이전 잘 작동하던 동작으로 완전 회귀)

### 버그 수정
- **자막이 바뀔 때마다 1프레임씩 화면이 비던 문제 해결.** 모든 자막 사이에 1프레임
  간격을 강제하던 로직을 제거하고, 이전에 잘 작동하던 것처럼 자막이 **정확히 맞닿도록**
  되돌렸습니다 — "빈틈없이 채우기"를 켜면 이제 전환 순간에도 화면이 비지 않습니다.
  (겹침 방지는 유지 — 경계가 프레임 격자에 스냅되므로 맞닿음은 겹침이 아닙니다)

## beta v0.12.56 — 2026-07-12 (프리뷰 글자 크기·위치를 파이널컷 실제 렌더와 일치)

### 버그 수정
- **프리뷰 글자 크기가 파이널컷과 2배 차이 나던 문제 해결.** 파이널컷 Subtitle 템플릿은
  4K(3840×2160) 씬을 프로젝트에 맞춰 축소 렌더하므로(실측), Font Size 70 = 1080p에서
  35px입니다. 프리뷰가 이 비율(conform)을 반영해 **실제 파이널컷 크기 그대로** 표시됩니다.
- 같은 원인으로 자막 **위치 이동량이 절반만 적용**되던 것도 교정했습니다(4K px 단위 반영).
- 위치 검증 프로젝트 2종을 수정된 수식으로 재생성: `~/Movies/FCP AutoCut/자막위치검증_가로·세로.fcpxmld`

## beta v0.12.55 — 2026-07-12 (빈틈없이 채우기 원래 동작 복원)

### 버그 수정
- **"빈틈없이 채우기"가 이름 그대로 동작하도록 복원.** v0.12.46~48에서 0.4초 이하의
  짧은 끊김만 메우도록 바뀌어 긴 쉼마다 자막이 사라졌습니다. 이제 옵션을 켜면 각 자막이
  **다음 자막이 나올 때까지** 화면에 남습니다(완전 메움 — 이전에 잘 작동하던 동작).
  자막 겹침은 별도의 '최소 1프레임 간격' 배치 로직이 계속 방지합니다.
  옵션을 끄면 기존대로 실제 발화 구간에만 자막이 표시됩니다.

## beta v0.12.54 — 2026-07-12 (자막 위치 어긋남 근본 해결 — 템플릿 실측 기반)

### 버그 수정
- **자막 위치를 조절하면 파이널컷에서 자막이 사라지던 문제의 근본 원인을 해결.**
  파이널컷 Subtitle 템플릿 원본(.moti)을 분석해, Y Position Offset이 **슬라이더 정규화
  값(0~1, 0.5=기본 위치, ±2000px 범위)**임을 확인했습니다. 기존 수식은 이를 '높이 비율'로
  잘못 해석해 위치를 조금만 조절해도 자막이 약 2000px 아래(화면 밖)로 렌더됐습니다.
  이제 프리뷰 위치가 그대로 파이널컷 위치가 됩니다 — 해상도·화면비 무관.
- 검증용 프로젝트 2종 제공: `~/Movies/FCP AutoCut/자막위치검증_가로·세로.fcpxmld`
  (미디어 불필요·임포트 가능, 위치 라벨 자막 묶음 — 한 화면에서 위치 확인)

## beta v0.12.53 — 2026-07-12 (모든 연결 방식의 오디오 자막화)

### 버그 수정
- **연결(Connect) 오디오를 어떤 방식으로 붙여도 모두 자막화.** 오디오를 커넥트 클립·연결
  스토리라인으로 타임라인에 붙인 경우, 연결 방식·중첩 깊이와 무관하게 **타임라인의 모든
  오디오**를 인식해 자막을 만듭니다 — 보조 스토리라인, 클립에 다시 연결된 클립(깊은 중첩),
  기본 스토리라인에 직접 lane 연결된 클립을 모두 지원합니다. 자막은 파이널컷이 허용하는
  기본 스토리라인 위치에 정확히 앵커됩니다.

## beta v0.12.52 — 2026-07-12 (자막 위치 해상도 무관 · 화면비 자동 판별)

### 버그 수정
- **자막 위치가 프로젝트 해상도와 무관하게 프리뷰 그대로.** 자막 Y 위치를 픽셀이 아니라
  **프레임 높이 비율**로 계산하도록 바꿔, 1080p·4K 등 어떤 해상도에서도 프리뷰에 보이는
  위치 그대로 배치됩니다. (아래로 내렸을 때 파이널컷에서 자막이 사라지던 문제 해결)

### 개선
- **입력 프로젝트 화면비 자동 판별.** 자막 스타일 프리뷰가 입력 프로젝트가 가로(16:9)인지
  세로(9:16)인지 자동으로 감지해 그 비율로 미리 보여줍니다. 가로 프로젝트에서는 세로(쇼츠)
  설정이 잠기고, 세로 프로젝트에서는 가로가 잠깁니다 — 실제 처리에 쓰이는 위치를 그대로
  편집하게 되어 위치 어긋남이 없습니다.

## beta v0.12.51 — 2026-07-12 (연결 스토리라인/연결 오디오 자막 지원)

### 버그 수정
- **연결 오디오·연결 스토리라인 자막 생성.** 오디오를 메인 스토리라인이 아니라 **연결 클립**
  이나 (‘스토리라인 만들기’로 만든) **연결 스토리라인**에 두면 "분석할 오디오 클립이 없습니다"
  로 실패하던 문제를 고쳤습니다. 이제 연결된 오디오도 찾아 자막을 생성합니다.
  - 자막은 **기본 스토리라인에 앵커**해 배치합니다 — 보조 스토리라인 내부 클립엔 파이널컷이
    앵커 항목을 허용하지 않아("Anchored items were ignored") 자막이 무시되던 문제도 해결.
  - 무음컷을 함께 켠 경우, 연결 오디오가 있으면 무음제거본은 안전하게 생략하고 자막본만
    생성합니다(타임라인 어긋남 방지). 무음컷 재정렬 지원은 다음에 다룹니다.

## beta v0.12.49 — 2026-07-12 (용어 사전 재설계 · 안정성 대폭 강화)

### 새 기능
- **용어 사전 재설계.** 자유 텍스트 대신 **단어별로** 등록합니다 — "등록 단어"와 "잘못된
  발음(오인식)"을 나란히 입력하고 개별로 추가·삭제합니다. **가져오기/내보내기(JSON)** 로
  백업·복원할 수 있습니다. 사이드바 "용어 사전 치환" 옆 톱니 버튼으로 설정을 바로 엽니다.
  (기본 켜짐 — 등록된 용어가 있을 때만 교정 검토 창이 뜹니다)
- **자막 스타일 화면비 기억.** 쇼츠(9:16)로 바꾸고 창을 나갔다 다시 열어도 마지막
  화면비가 유지됩니다.

### 변경
- **"다시 말하기 컷"을 잠시 숨겼습니다** — 기능을 더 다듬어 다음 버전에 다시 선보입니다.

### 안정성 (내부 감사 반영)
- App Store판에서 프로젝트가 참조하는 미디어 폴더 접근(보안 스코프)을 지원합니다.
- 자막본을 같은 라이브러리로 다시 가져올 때 프로젝트 UID 충돌을 방지하고, 파일에 프로젝트가
  여러 개면 첫 프로젝트만 처리합니다.
- 프로젝트에 있던 **사용자 타이틀·캡션을 앱 자막과 구분해 보존**합니다.
- 생성된 FCPXML이 규격(DTD) 검증에 실패하면 깨진 결과물을 내놓지 않고 작업을 실패 처리합니다.
- 결과물 일부가 누락됐는데 완료로 표시되던 문제, 빈·손상 파일 입력, 빠른 연속 드롭·두 창
  동시 실행을 방어합니다.
- 작업 취소 시 프로세스 강제 종료·임시 파일 정리, 업데이트 다운로드 무결성(HTTP·크기·DMG)
  검증, 모델 다운로드 전 저장 공간 확인·다운로드 중 삭제 방지를 추가했습니다.
- 구매 상품 로드 실패 시 재시도 경로, 작업 히스토리의 결과 폴더 접근 권한 복원을 개선했습니다.

### 알려진 이슈 (수정 진행 중)
- **자막 하단 위치:** 프리뷰에서 자막을 아래로 많이 내리면 파이널컷에서 보이지 않을 수
  있습니다 — 위치를 조절하지 않으면(기본) 영향 없습니다. 프리뷰↔파이널컷 위치 정합을
  조사 중입니다.
- **30p 프로젝트:** 29.97fps 소스를 30p 프로젝트로 처리하면 가져오기 시 "edit frame
  boundary" 경고가 다수 표시될 수 있습니다(표시는 정상 동작 가능).

## beta v0.12.48 — 2026-07-12 (자막 스타일 프리뷰 — 쇼츠 대응)

### 새 기능
- **자막 스타일 프리뷰.** 사이드바 "자막 스타일"을 누르면 1080p 비율 프리뷰가 열립니다:
  - **16:9 ↔ 9:16(쇼츠)** 전환, 자막을 **직접 드래그**해 위치 조절
  - **글자 크기 슬라이더** — 30~150pt, 10단위. 글자가 커져도 프리뷰를 벗어나지 않게
    위치가 자동 보정됩니다
  - **자막 길이 10구간** — '단어'(~6자, 쇼츠식 한 단어 자막)부터 '아주 길게'(~50자)까지.
    표기는 Title Bible 기준(목표 min–max자)이며, 실제 분리는 항상 의미 단위·자연
    경계가 우선입니다. 프리뷰 표본이 실제 분리 엔진 산출로 구간마다 변합니다
  - **2줄 자막** — 한 카드를 균형 잡힌 두 줄로(담는 양 ×1.5). 줄바꿈은 의미 경계에서만,
    두 줄 길이는 최대한 비슷하게(TED 원칙)
  - **위치는 16:9와 9:16 별도 저장** — 처리 시 입력 프로젝트의 화면비를 자동 감지해
    맞는 위치를 적용합니다
- **9:16 플랫폼 세이프존 가이드 (2026 실무 기준).** 유튜브·인스타그램·틱톡·공통 토글 —
  각 플랫폼 UI에 가려지는 상·하·좌·우 위험 구역과 **핵심 안전영역(780×1200px)** 점선을
  표시합니다. 9:16 기본 자막 기준선도 3사 공통 세이프존(Y 1320px)으로 설정됩니다.

### 개선
- 자막 글자 크기가 파이널컷 인스펙터의 Font Size 값과 일치하도록 템플릿 파라미터에도
  반영됩니다 (FCP 12.3 Subtitle).

### 알려진 이슈 (수정 진행 중)
- **화면 위치 지정(실험적):** 위치를 조절한 경우 일부 환경에서 자막이 화면 밖으로
  벗어날 수 있습니다 — 위치를 조절하지 않으면(기본) 영향 없습니다. 문제가 보이면
  프리뷰의 "기본 위치로"를 누르고 재처리하세요.
- **30p 프로젝트:** 29.97fps 소스를 30p 프로젝트로 처리하면 가져오기 시 "edit frame
  boundary" 경고가 다수 표시될 수 있습니다(표시는 정상 동작 가능). 다음 버전에서
  미디어 프레임 격자 스냅으로 수정 예정입니다.

## beta v0.12.47 — 2026-07-11 (파이널컷 12.3 자막(Subtitle) 정식 지원)

### 새 기능
- **FCP 12.3 기본 자막(Subtitle) 템플릿으로 생성.** 타이틀 선택이 **FCP 12.3 Subtitle /
  FCP Custom** 두 가지로 정리됐고, 설치된 파이널컷 버전에 맞는 템플릿 내부 값으로
  생성합니다. 네이티브 내보내기와 필드 단위 대조로 템플릿 시작점을 정확히 맞춰
  (타임코드 1시간 = 실제 3603.6초 — 외부 도구들이 3600초로 하드코딩해 어긋나던 근본
  원인 규명) 교체·애니메이션 이상이 사라졌습니다.
- **"Subtitles 롤" 체크 (FCP 12.3 전용).** 켜면 자막에 파이널컷 12.3의 Subtitles 롤을
  부여해 '모든 자막 선택'과 인스펙터 일괄 스타일 편집의 대상이 됩니다. 기본은 꺼짐
  (일반 Titles 롤).
- **물음표 자동 부여** — 확실한 의문문에만 '?'를 붙입니다(서술문 오탐 0, 방송 관행).
- **처리 중 실시간 인식 텍스트 표시** — 음성이 인식되는 대로 화면에 흐릅니다.

### 개선
- 사이드바 드롭다운 균일 폭 정렬, 사이드바 폭 완전 고정(드래그 확장 차단),
  창 최소 높이 보장(메뉴 가림·스크롤 방지), 하단 상태줄 배경 정리.
- 자막 최소 1초 표출 보장(뒤 침묵으로만 확장).

### 정리
- 'AI 의미 분리'와 8GB 언어모델 제거 — 실측 결과 어색한 줄바꿈의 원인은 언어모델이
  아니라 글자수 상한이었고(소프트 상한으로 해결), 규칙 기반 분리만 남깁니다.

## beta v0.12.46 — 2026-07-11 (긴 침묵 자막 잔류 수정 — 겹침 연쇄 해결)

### 수정
- **긴 침묵 구간에 이전 자막이 계속 떠 있던 버그 수정.** '빈틈없이 채우기'가 설명
  (≤0.4초 간격만 이음)과 달리 **모든 간격을 무조건** 이어붙여, 발화가 없는 구간에도
  자막이 남아 있었고 전체 자막이 서로 맞닿아 파이널컷에서 조작 시 겹침·위로 쌓임이
  연쇄되던 근본 원인이었습니다. 이제 짧은 깜빡임만 잇고 긴 침묵은 자막을 내립니다.
- **자막 사이 최소 1프레임 간격 보장** — 정확히 맞닿은 경계가 만들던 겹침 판정을
  원천 차단합니다 (실측 검증된 해결책).

## beta v0.12.45 — 2026-07-11 (문장 경계 강화 + 글자수 여유)

### 수정
- **질문 문장은 반드시 `?`로 끝나고 즉시 다음 자막으로 넘어갑니다.**
  `오늘 무엇을 해볼까요 다음으로…`처럼 쉼 없이 이어 말해도
  `오늘 무엇을 해볼까요?`에서 자막을 확정하며, 물음표 뒤에 다른 텍스트가
  같은 자막으로 붙지 않습니다. 타이틀·캡션·SRT 모두 같은 규칙을 사용합니다.
- **자막 길이에 여유를 더 줬습니다.** 짧게 24자 / 보통 32자 / 길게 40자를
  소프트 목표로 사용하고, 자연스러운 의미 경계까지 최대 8자를 추가 허용합니다.
- **문장 끝과 다음 문장 시작의 혼합을 불변조건으로 금지했습니다.** 글자수 상한이
  넉넉해도 문장 경계는 합치지 않으며, `.?!` 뒤 텍스트가 같은 카드에 남으면 테스트와
  런타임 검증이 실패하도록 강화했습니다. Title Bible을 v3 기준으로 재정리했습니다.

## beta v0.12.44 — 2026-07-11 (자막 분리 안정화 — 분리점 인덱스 구조)

### 수정
- **자막 분리기가 원문을 직접 만들지 않고 단어 분리점만 고르도록
  재구성했습니다.** 선택된 단어 인덱스를 기준으로 코드가 원문·타임코드를
  재조립하므로, 단어 누락·중복·순서 변경이 생기지 않습니다.
- **짧은 명사의 끝글자를 조사·연결어미로 오인하던 문제를 수정했습니다.**
  `광고`·`사고`·`아이`·`사과` 같은 단어에서 불안정한 분리 후보가 생기지 않습니다.

## beta v0.12.43 — 2026-07-11 (타이틀 교체 어긋남 해결 — 타이틀 스타일 선택)

### 새 기능
- **타이틀 스타일 선택 — Vrew·Whisper류 도구의 고질적인 '타이틀 교체 시 타임라인
  어긋남'을 원천 해결.** 외부 도구로 만든 자막을 파이널컷에서 다른 타이틀로 교체하면
  길이가 템플릿 기본값으로 리셋되며 자막이 부풀고 어긋납니다 — 원인은 Motion 템플릿마다
  다른 내부 시작점(start)을 외부 XML이 3600s로 하드코딩하기 때문입니다(FCP 네이티브
  파일과의 정밀 비교로 규명). FCP AutoCut은 사이드바 **타이틀 스타일**에서 원하는
  템플릿을 골라 **처음부터 그 템플릿의 네이티브 값으로 생성**하므로 교체가 필요 없고
  어긋남이 생기지 않습니다.
  - **기본 (그림자)**: Basic Title — 굵게 + 그림자 (기존과 동일)
  - **Custom (외곽선)**: FCP 내장 Custom — 검정 외곽선 하단 자막 (예능·유튜브 스타일)

## beta v0.12.42 — 2026-07-10 (자연스러운 줄바꿈 대폭 개선 + 물음표 + 실시간 인식)

### 개선
- **자막 줄바꿈이 훨씬 자연스러워집니다.** 글자수 상한을 '소프트'하게 바꿔, 조사·부사·
  관형형 중간을 억지로 끊지 않고 문장·연결어미 같은 **자연 경계**에서 끊습니다. 같은
  영상 기준 어색한 분리가 절반 가까이 줄었습니다(48%→28%). 자막 길이 프리셋도 단일
  줄 의미 단위에 맞게 조정(짧게 20 / 보통 27 / 길게 34자).
- **질문 자막에 물음표(?)가 붙습니다.** "계신가요", "어떻게 할까요"처럼 확실한 의문문만
  물음표를 부여하고, 서술문에는 붙이지 않습니다(방송 자막 관행).
- **처리 중 인식된 텍스트가 실시간으로 표시됩니다.** 음성이 인식되는 대로 화면에
  텍스트가 흐릅니다.
- **자막 최소 표출 시간 1초 보장** — 너무 짧게 스쳐 지나가던 자막을 뒤 침묵으로 늘려
  읽을 시간을 확보합니다(무음 구간이 있을 때만).

### 정리
- **'AI 의미 분리' 기능과 8GB 언어모델(Qwen 14B)을 제거했습니다.** 실측 결과 어색한
  줄바꿈은 언어모델이 아니라 글자수 상한이 원인이었고(위 소프트 상한으로 해결), 로컬
  모델은 원문을 훼손하는 부작용이 있어 규칙 기반 분리만 남깁니다. 다운로드 용량·처리
  시간이 줄고 앱이 단순해집니다. (기본 동작은 원래 규칙 분리라 변화 없음)

### 수정
- 문제 진단(리포트) 시 파이프라인 실패 원인을 구체적으로 표시하고, DMG에서 바로 실행해
  생기는 격리 실행(App Translocation) 문제를 감지해 "Applications로 옮기라"고 안내합니다.

## beta v0.12.41 — 2026-07-10 (사이드바 드롭다운·폭 고정 수정)

### 수정
- **사이드바 드롭다운(자막 형식·글자 크기·자막 길이 등)을 처음 열 때 항목 정렬이
  어긋나고 뒤 토글이 비쳐 보이던 문제 수정.** 커스텀 메뉴 대신 macOS 네이티브
  드롭다운으로 통일해, 첫 열림부터 항목이 바르게 정렬되고 선택 표시(체크마크)도
  정상적으로 나타납니다.
- **창을 키우면 사이드바(서랍)가 함께 넓어지던 문제 수정.** 사이드바 폭을 완전히
  고정(252pt)해, 창 크기와 무관하게 항상 같은 너비를 유지합니다. 여닫기는 서랍
  버튼으로만 가능합니다.

## beta v0.12.40 — 2026-07-10 (작업 실패 긴급 수정)

### 수정
- **v0.12.37~39에서 작업이 실패하던 문제 긴급 수정.** 음성 인식 모델을 미리 준비하는
  최적화(v0.12.37)가 내부 그래픽 처리 방식과 충돌해 전사 단계에서 실패하고 있었습니다.
  해당 최적화를 제거해 정상 동작을 복구했습니다. (다른 개선은 그대로 유지)

## beta v0.12.39 — 2026-07-10 (음성 인식 속도·품질 개선)

### 개선
- **자막 인식이 더 빠르고 깔끔해집니다** — 짧게 나뉜 음성 구간을 하나로 합쳐 음성
  인식 호출 횟수를 줄였습니다. 처리 시간이 줄고, 구간 경계에서 단어가 중복되던
  드문 오인식도 함께 줄어듭니다.

## beta v0.12.38 — 2026-07-10 (큰 쉼에서 자막 분리 — 미리뜸 수정)

### 수정
- **다음 발화가 앞 자막에 미리 뜨던 문제 수정.** 말 사이에 큰 쉼(약 0.8초 이상)이
  있으면 자막을 나눕니다. 이전에는 "써요"처럼 종결어미로 인식되지 않는 구어 뒤에
  긴 쉼이 오면, 다음 문장("안녕하세요")이 앞 자막에 붙어 실제 발화보다 먼저
  나타나던 문제가 있었습니다. (해당 파일은 다시 처리해야 반영됩니다)

## beta v0.12.37 — 2026-07-10 (모델 예열로 시작 단축)

### 개선
- **AI 모델을 미리 준비** — 오디오 추출·음성 구간 분석이 도는 동안 음성 인식 모델을
  백그라운드로 함께 로드해, 실제 자막 생성 시작까지 걸리는 시간을 줄였습니다.

## beta v0.12.36 — 2026-07-10 (무음제거 속도 개선)

### 수정
- **무음컷이 있는 긴 영상의 처리 속도 개선** — 무음 제거본을 만들 때 자막을 조각에
  배정하는 계산이 불필요하게 반복되던 것을 한 번만 하도록 고쳐, 컷이 많은 긴
  타임라인에서 처리 시간이 크게 줄어듭니다. (결과물은 동일)

## beta v0.12.35 — 2026-07-10 (앱 아이콘 교체)

### 변경
- **새 앱 아이콘** (Icon Composer, 에셋 카탈로그 방식 — MAS 호환).

## beta v0.12.34 — 2026-07-10 (앱 아이콘)

### 신규
- **앱 아이콘 추가** — 클래퍼보드 디자인의 새 앱 아이콘. (Dock·Finder·앱 전환에 표시)

## beta v0.12.33 — 2026-07-10 (업데이트 시각 알림)

### 신규
- **업데이트 배지** — GitHub에 새 버전이 있으면 상단 툴바에 강조색 다운로드 배지가
  지속 표시됩니다(배너를 닫아도 남음). 눌러서 바로 업데이트할 수 있습니다.

## beta v0.12.32 — 2026-07-10 (유지보수)

### 참고
- 앱 내 자동 업데이트("지금 업데이트" 버튼) 동작 확인용 릴리스. 기능 변경 없음.

## beta v0.12.31 — 2026-07-10 (앱 내 자동 업데이트)

### 신규
- **"지금 업데이트" 버튼** — 새 버전이 있으면 상단 배너의 버튼을 눌러 최신 버전을
  앱이 직접 내려받고(진행률 표시) 자동으로 엽니다. 뜬 창에서 앱을 Applications로
  드래그하면 설치 완료. (더 이상 다운로드 페이지를 직접 찾을 필요 없음.)

## beta v0.12.30 — 2026-07-10 (사이드바 고정 + FCPXML 경량화)

### 개선
- **사이드바 폭 고정** — 드래그로 임의 조절되지 않고(스크롤바 방지) 토글 버튼으로만
  여닫습니다. 항상 라벨이 온전히 보이는 폭으로 고정.
- **긴 타임라인 안정성 — FCPXML 경량화.** 자막마다 만들던 중복 스타일 정의를 하나로
  통합해, 2시간짜리 프로젝트도 파일 크기·FCP 메모리 부담이 크게 줄어듭니다.
  (자막 내용·한글 폰트·화자색·위치는 그대로.)

## beta v0.12.29 — 2026-07-10 (드롭다운·사이드바 UI 수정)

### 수정
- **드롭다운 메뉴가 비어 보이던 문제 수정.** 자막 형식·글자 크기·자막 길이 드롭다운을
  열면 항목이 안 보이던 버그(macOS 렌더 이슈)를 고쳐, 이제 모든 항목이 정상 표시됩니다.
- **첫 실행 시 사이드바가 너무 좁아 라벨이 잘리던 문제 수정.** 사이드바 최소 폭과 창
  기본 크기를 지정해 처음부터 라벨이 온전히 보입니다.

## beta v0.12.28 — 2026-07-09 (무음제거본 중복 자막 수정 + AI 모델 목록 갱신)

### 수정 — 무음제거본에서 자막이 중복되던 문제
- **무음 컷에 걸친 자막이 두 번 표시되던 오류 수정.** 문장 중간에서 무음이 제거되면
  그 자막이 컷 앞·뒤 조각에 **똑같이 두 번** 찍혀, 앞 자막에 미리 뜨는 것처럼 보였습니다.
  이제 자막을 **단어 단위로 조각에 나눠** 컷 앞에는 앞부분, 뒤에는 뒷부분만 표시합니다
  (중복·누락 없음). 실제 인터뷰 결과물에서 61종 중복이 발생하던 문제입니다.

### 변경
- **설정 → AI 모델**에 "Qwen2.5 언어 모델 (AI 의미 분리)" 표시 추가. AI 의미 분리를
  스크립트 기반·자동으로 쓸 때 받는 선택 모델의 상태·용량을 확인/관리할 수 있습니다.

## beta v0.12.27 — 2026-07-09 (AI 의미 분리 3-모드 + 콘텐츠 자동 판별)

### 신규 — "AI 의미 분리"가 3가지 모드 선택으로 바뀜
- **스크립트 없음**(기본): 규칙 분리만 — 빠르고 대부분의 경우 충분.
- **스크립트 기반**: 항상 AI로 다듬음 — 대본·낭독 영상에 적합.
- **자동**: 전사문을 보고 **자유발화 vs 낭독을 스스로 판별**해, 낭독일 때만 AI를 적용.
  자유 대화(군말·구어체가 많은 인터뷰 등)는 규칙 분리로 빠르게 처리 → **불필요한 대기 시간 제거**.

판별은 담화표지·간투어 밀도, 종결어미 격식, 문장 길이 같은 규칙 신호로 하며,
실측에서 자유발화 인터뷰(84점)와 낭독 내레이션(12점)이 뚜렷이 구분됩니다.

## beta v0.12.26 — 2026-07-09 (소요 시간 표시 + 타임코드 라이트 모드)

### 신규
- **작업 소요 시간 표시** — 완료된 결과에 걸린 시간을 "소요 시간 3분 12초"로 보여줍니다.

### 수정
- **타임코드 표시 라이트 모드 지원** — 진행 중 나오는 타임코드·레벨미터 박스가
  다크 모드만 지원하던 것을 라이트 모드에서도 자연스럽게 보이도록 배경·글자·미터 색을
  테마에 맞춰 바꿨습니다.

## beta v0.12.25 — 2026-07-09 (사이드바 섹션 이름 정리)

### 변경
- 사이드바 **"고급" → "자막 다듬기"**. 화자 분리·용어 사전 치환·AI 의미 분리가
  모두 자막을 더 정교하게 다듬는 선택 옵션임을 이름으로 드러냈습니다.

## beta v0.12.24 — 2026-07-09 (AI 의미 분리 실동작 수정·튜닝 + 이름 정리)

### 수정
- **AI 의미 분리가 실제로 동작하도록 수정** — 내부에서 삭제된 옛 함수를 참조해,
  켜도 항상 규칙 분리로 되돌아가던 문제(v0.12.23)를 고쳤습니다.
- **과분리 튜닝** — 언어모델이 자막을 지나치게 잘게 쪼개던 경향을 줄여, 카드를
  글자수 상한까지 꽉 채우고 꼭 필요한 곳에서만 나눕니다(고유명사·보조용언 보존 강화).

### 이름 변경 (기능 동일)
- **"AI 자막 교정" → "용어 사전 치환"**, 설정 탭 **"자막 인식 향상" → "용어 사전 치환"**.
  실제로 AI가 아니라 등록한 고유명사·용어 목록으로 자막을 치환·통일하는 기능임을 명확히 했습니다.

## beta v0.12.23 — 2026-07-09 (AI 의미 분리 옵션 추가 — opt-in)

### 신규 — 규칙 분리를 언어모델이 의미 단위로 다듬는 옵션
- **고급 → "AI 의미 분리 (느림)"** 토글 추가. 규칙 기반 자막 분리를 언어모델
  (Qwen2.5-14B)이 문장 전체 맥락으로 읽고 나열·수식 경계 등을 더 자연스럽게 다듬습니다.
  (예: "짧은 문장 / 작은 관심 따뜻한 질문 하나가"처럼 나열을 항목 단위로.)
- **안전장치** — 원문·타임코드는 절대 바뀌지 않으며(단어 텍스트·시간 고정), 모델
  출력이 검증(원문 일치·글자수·금지 규칙)을 통과할 때만 채택하고 실패 시 규칙 분리를
  그대로 유지합니다. 과분리·고아 카드는 자동 보정.
- **비용** — 기본 꺼짐. 켜면 문장마다 AI를 돌려 느리고(30분 영상 10분+), 최초 1회
  약 8GB 모델을 내려받습니다. 벤치마크에서 원문보존 100%로 후보 중 최적이었습니다.

## beta v0.12.22 — 2026-07-09 (자막 분리 의미경계 보강)

### 개선 — 실제 인터뷰 검수로 발견한 부자연 분리 수정 (문장별 예외 아님, 일반 규칙)
- **시간·관계 의존명사를 앞말에 붙임** — "경험해 보기 **|** 전에", "말씀하신 **|** 대로",
  "마치셨을 **|** 때"처럼 전에·후에·뒤에·때·중·동안·대로 등이 다음 자막으로 넘어가던
  문제 수정.
- **"수 있도록 / 만하다" 결합 보존** — "작업할 수 **|** 있도록", "붙잡을 **|** 만한"을
  쪼개지 않습니다.
- **명사 오분리 방지** — "먹어 **|** 대형", "마치고 **|** 보고서"처럼 명사(대형·대한민국·
  보고서)를 보조용언으로 오인해 붙이던 내부 버그 수정.
- 실측: 28.7분 인터뷰(489자막) 경계 위반 0 · 글자수 상한 초과 0.

## beta v0.12.21 — 2026-07-09 (자막 분리 엔진 전면 개편 — DP 전역 최적화)

### 개편 — 타이틀 분리를 문장별 예외 없는 일반 로직으로
- **의미 단위 분리 엔진 신규** — 문장을 왼쪽부터 훑어 자르던 방식(문장마다 예외
  발생)을 걷어내고, 문장 전체에서 "가장 자연스러운 분리 조합"을 계산하는 방식으로
  교체했습니다. 절·구·조사 경계의 우선순위를 종합해 전역 최적을 찾습니다.
- **부자연스러운 분리가 구조적으로 사라짐** — 보조용언("말하고 싶은"), 의존명사
  ("얻을 수 있습니다"), 숫자+단위("열한 명"), 관형형+명사를 쪼개지 않고, 화면을
  넘는 과대 카드도 생기지 않습니다.
- 실측: 9분 내레이션(1,321단어→270장) 전부 글자수 상한 이내, 부자연 분리 0.

## beta v0.12.20 — 2026-07-09 (자막 분리 자연스러움 개선)

### 개선 — 타이틀이 문장 중간에서 부자연스럽게 잘리는 문제 3가지 수정
- **보조용언을 쪼개지 않습니다** — "말하고 · 싶은", "먹어 · 버렸다", "해 · 보고"처럼
  하나의 서술어를 이루는 보조용언(-고 싶다/있다, -어 보다/주다/버리다/놓다/두다)
  사이에서 자막이 갈라지지 않습니다.
- **화면을 넘는 과대 카드 제거** — 끊을 곳을 못 찾아 한 카드가 글자수 상한을
  크게 넘던 문제를 해결(다소 짧은 뒷카드를 감수하고라도 자연스럽게 나눔).
- **문장 경계 인식 개선** — "안녕하세요"처럼 종결어미 뒤에 "오늘/오빠"류 단어가
  오면 이동보조("갔다 왔어")로 오인해 두 문장을 한 카드에 붙이던 버그 수정.

## beta v0.12.19 — 2026-07-09 (AI 자막 교정 속도 대폭 개선)

### 개선
- **AI 자막 교정이 이제 즉시 처리됩니다** — 설정의 고유명사·용어 목록으로 자막의
  고유명사·기술 용어·약어를 일관되게 통일합니다. 30분 영상도 몇 초 이내. (이전엔
  문장마다 AI 모델을 돌려 매우 오래 걸렸습니다.)
- **추가 다운로드 불필요** — 이 교정에는 대용량 모델을 쓰지 않습니다. 설정에
  용어를 입력하면 조사까지 보존해 정확히 통일합니다(예: "제리안이" → "정예린이").

## beta v0.12.18 — 2026-07-09 (자막 과분리 개선 + 빈틈없이 채우기 강화)

### 이름 변경
- **"AI 문맥 교정" → "AI 자막 교정"** — 자막의 맥락을 이해하여 고유명사·기술 용어·약어
  등을 교정하고 일관되게 통일합니다.

### 버그 수정 / 개선
- **문장 중간에서 자막이 잘리던 문제 완화** — 반말·구어 종결어미(예 "…상태야",
  "…거든", "…잖아")를 (뒤 호흡이 있을 때) 문장 끝으로 인식합니다. "자 이게 녹화되는
  상태야"가 "녹화되는 / 상태야"로 쪼개지지 않고 한 자막에 붙습니다.
- **"빈틈없이 채우기"가 진짜 빈틈없이** — 켜면 각 자막이 **다음 자막이 나올 때까지**
  화면에 남아 문장 사이 긴 쉼의 공백도 채웁니다. (끄면 각 자막이 발화 구간만
  정확히 덮음)

## beta v0.12.17 — 2026-07-09 (정리 — 사장 코드 제거 + 검증 강화)

### 정리 (Claude·Codex 교차검증 감사 기반)
- **동작하지 않던 CLI 옵션 제거** — 내부 파이프라인이 조용히 무시하던 8개 옵션을
  걷어냈습니다(혼란 방지). 실제 AI 교정은 "AI 문맥 교정" 옵션으로 동작합니다.
- **사장 코드 제거** — 앱 경로에서 쓰이지 않는 구 모듈(미사용 JSON-RPC 서버,
  구 자막 편집 모듈, 스타일 참조)을 삭제해 유지보수성을 높였습니다.
- **문맥 교정 회귀 테스트 추가** — 조사 보존·문장 조립·**타임코드 불변** 등 안전
  불변식을 자동 검증(모델 없이 실행).

(기능 변화 없음 — 자막·무음컷·AI 문맥 교정 동작은 v0.12.16과 동일)

## beta v0.12.16 — 2026-07-09 (AI 문맥 교정 — 사전 승인 팝업)

### 새 기능
- **교정 사전 승인 팝업** — AI 문맥 교정을 켜면, 이제 **적용 전에 검토 창**이 뜹니다.
  각 교정(원문 → 교정)을 보고:
  - 체크로 **수락/거부**, 값을 **직접 수정**
  - "모두 켜기/끄기", "교정 없이 진행", "적용하고 계속"
  - 승인한 것만 자막에 반영됩니다 (타임코드는 그대로).
  STT를 다시 돌리지 않고 검토 후 이어서 진행합니다. (실제 음성으로 정지→승인→
  재개 흐름 검증 완료)

## beta v0.12.15 — 2026-07-09 (문맥 교정 실행 버그 + 화자분리 다중소스 수정)

### 버그 수정
- **AI 문맥 교정이 실행되지 않던 문제** — 교정 옵션이 "무음컷"과 잘못 묶여 있어,
  무음컷을 끄고 자막만 만들면 교정이 아예 돌지 않았습니다. 자막 작업에 정상 연결.
- **다중 소스에서 화자가 섞이던 문제** — 화자 분리가 첫 번째 소스만으로 판정해
  다른 소스 클립의 화자를 잘못 배정했습니다. 이제 **소스(미디어)별로 각각 화자를
  분리**합니다. (같은 소스를 여러 번 오디오 추출하던 비효율도 함께 해결)

### 개선 (내부)
- AI 문맥 교정이 한 단어를 여러 단어로 바꾸지 않도록 방지 (타임코드 1:1 보존).
- 파이프라인 단계 순서를 Claude·Codex 교차 검증으로 점검.

## beta v0.12.14 — 2026-07-09 (AI 문맥 교정 — 실험)

### 새 기능 (실험, opt-in)
- **AI 문맥 교정** — 사이드바 **고급 → "AI 문맥 교정"** 을 켜면, STT가 잘못 들은
  단어를 문맥으로 교정합니다.
  - **고유명사**는 설정(⌘,)의 고유명사 목록으로 정확히 교정 (예: `정예린(제리 아님)`)
  - **깨진 단어**는 AI가 문맥을 읽고 추론 (7B 로컬 모델 — 실측: 정상 단어는 안 건드리고
    깨진 단어를 정확히 교정)
  - 결과 폴더에 **`⋯_AI교정내역.md`** 리포트(무엇을 왜 고쳤는지) 생성
  - 단어 텍스트만 교체 — **타임코드·컷·자막 배치는 그대로**
  - 기본 꺼짐. 켜면 최초 1회 음성 모델 외 **7B 모델(약 4GB)** 을 내려받습니다.
  - (교정 전 승인 팝업은 다음 버전에서 추가 예정 — 현재는 자동 적용 + 리포트 검토)

## beta v0.12.13 — 2026-07-09 (사이드바 토글·드롭다운 다듬기)

### 버그 수정 (UI)
- **사이드바 접기 애니메이션 버벅임 수정** — 토글이 이중 애니메이션으로 끊기던
  문제를 고쳐, macOS 네이티브 슬라이드로 부드럽게 접힙니다.
- **드롭다운 팝업 정렬 수정** — 선택 메뉴를 처음 열 때 항목·체크마크가 어긋나던
  문제 해결 (첫 열림·재열림 항상 동일하게 정렬).

## beta v0.12.12 — 2026-07-09 (사이드바·툴바 정렬 다듬기)

### UI
- **앱 이름 위치** — `FCP AutoCut`을 신호등(창 버튼) 바로 오른쪽, 타이틀바 줄에
  배치했습니다.
- **툴바 아이콘 정리** — 열기·작업 히스토리·항상 위에 표시·설정 아이콘을 창
  오른쪽 끝으로 옮기고 각각 개별 버튼으로 분리했습니다. 사이드바 접기 버튼은
  왼쪽에 그대로 둡니다.
- **드롭다운 정렬** — 언어·자막 형식·글자 크기·자막 길이·컷 강도 드롭다운을
  **모두 같은 너비로 통일**하고 오른쪽에 나란히 정렬했습니다.

## beta v0.12.11 — 2026-07-09 (사이드바 디자인 정리)

### UI
- **옵션 스위치 전환** — 사이드바의 자막 자동화·빈틈없이 채우기·무음컷·군소리 컷·
  다시 말하기 컷·화자 분리를 체크박스에서 **우측 정렬 토글 스위치**로 바꿨습니다.
- **드롭다운 크기 통일** — 언어·자막 형식·글자 크기·자막 길이·컷 강도 드롭다운을
  **모두 같은 너비**로 맞춰 정렬을 정리했습니다.

## beta v0.12.10 — 2026-07-09 (툴바 재구성 + 설정 UI 개선)

### UI
- **툴바 재구성** — 앱 이름 `FCP AutoCut`을 창 좌측 상단(사이드바 헤더)으로
  옮기고, 사이드바 접기/펼치기 버튼을 그 옆에 배치했습니다. 우측 액션은
  열기 · 작업 히스토리 · 항상 위에 표시 · 설정 순으로 정렬했습니다.
- **설정 › 일반 — 고유명사 적용 버튼** — 발음이 헷갈리는 고유명사·전문용어를
  입력한 뒤 **적용** 버튼으로 반영합니다. 반영 전에는 테두리가 파랗게 표시되고,
  반영 후 "적용됨 (N개 용어)"으로 확인됩니다.
- **설정 › AI 모델 — 모델 카드** — 각 모델의 이름·역할·실제 디스크 용량과
  설치 여부(다운로드됨/미설치)를 카드로 보여줍니다.

### 변경
- **AI 자막 분리(실험)를 이번 릴리스에서 제외** — 문맥 기반 LLM 분리는 아직
  채택률이 낮아, 필수 다운로드 모델에서 제외했습니다. 다운로드 용량이
  약 4.5GB → **약 3.5GB(2개 모델)**로 줄었습니다. 자막 분리는 검증된 규칙
  기반으로 동작합니다.

## beta v0.12.9 — 2026-07-07 (문어체 문장 경계 + 컷 검토 마커 + 판정 통합)

### 버그 수정
- **낭독체에서 "…펼쳤다 그는"처럼 문장 끝과 다음 문장 시작이 한 자막에 섞이던
  문제 수정** (Title Bible §4.2 위반) — 종결어미 인식이 구어체(~습니다/~어요)
  중심이라 문어체 과거("~ㅆ다": 펼쳤다/했다/도착했다)를 문장 끝으로 보지
  못했습니다. 이제 **뒤 호흡(0.3초+)과 함께** 문장 끝으로 인식합니다.
  ("갔다 왔어요" 같은 붙어 발화된 관용구는 끊지 않음 — KsponSpeech 62만 발화
  실측 기반 설계)
- 이 수정으로 자막이 컷 제거 구간을 가로지르며 타이밍이 밀려 보이던 문제도
  함께 해소됐습니다.

### 새 기능 — 컷 검토 (오컷 안전망)
- **무음제거본 타임라인의 각 컷 지점에 마커가 표시됩니다** — 잘린 내용과
  사유(군소리/다시 말하기)를 FCP에서 마커로 훑으며 검토하고, 필요하면 원본에서
  되살릴 수 있습니다.
- **컷 내역 리포트** — 결과 폴더에 `⋯_컷내역.md`가 생성됩니다 (원본 시간·유형·
  잘린 텍스트).

### 버그 수정 (화자 분리)
- **화자 분리를 켜면 작업이 실패하던 문제 수정** ('PosixPath' object has no
  attribute 'read') — 화자 분리에 문제가 생겨도 이제 작업 전체가 멈추지 않고
  화자 분리 없이 계속 진행합니다.
- 앞말에 붙어 발화된 짧은 파편("…거든 | 요")이 다른 화자로 오인되어 문장
  중간이 찢기던 문제 수정.

### 개선 (자막 분리 정확도 — 실인터뷰 19분 검증: Bible 위반 20→10건)
- **구어 종결어미(~세요/~게요/~고요/~래요 등) 인식** — "잠시만요 시작하겠습니다"
  처럼 인터뷰체 문장이 안 끊기던 문제. KsponSpeech 실측(문장끝율 70~91%)에 따라
  뒤 호흡(0.3초+)이 있을 때만 문장 끝으로 봅니다 (Bible §4.2 3단계 판정).
- **자막 없는 오디오 꼬리 제거** — 무음제거본의 유지 구간을 자막 범위(±0.3초)로
  다듬어 웃음·잡음이 자막 없이 남지 않습니다.
- 종결어미·추임새 판정을 자막 분리와 자동 커팅이 **단일 기준으로 공유**하도록
  통합 (이중 기준 제거).
- 군소리 어휘에 한숨·헛웃음(하/허/휴) 추가 — KsponSpeech 용례 확인.
  "응"(대답)·"아이"(명사)는 오컷 위험으로 제외.

### UI
- **작업 타이머** — 실행 순간부터 파이널컷 타임코드 스타일(HH:MM:SS:FF)로
  경과 시간이 표시됩니다.
- **오디오 레벨미터** — 진행률 100%가 될 때까지 FCP 미터 모양으로 살아 움직이는
  활동 표시가 추가됐습니다.

### 검증
- 회귀 테스트 37건 (KsponSpeech 실화자 사례·구어 종결·화자 턴 포함) 전부 통과.

## beta v0.12.8 — 2026-07-07 (완전 문장 리테이크 컷 + 무음 공백 제거)

### 새 기능 — 최종 테이크 채택 (last-take selection)
- **문장을 완독한 뒤 다시 읽으면 앞선 테이크를 통째로 삭제합니다** —
  "다시 말하기 컷"의 정의를 "다시 말한 부분 이전을 모두 삭제"로 확장
  (Title Bible §16.3에 발화 수리 용어·규칙 정식 수록: 리테이크·리파란덤·
  편집구·수리부).
  - 오컷 방어 3중: 첫 2어절 동일 + 4어절 이상 / 치환 어절의 글자 유사성
    ("또 다시"→"또다시" 허용, "학교에"→"교회에"는 다른 문장으로 거절) /
    3회 반복 시 마지막 테이크만 채택
  - "감사합니다 감사합니다" 같은 짧은 강조 반복은 계속 보존

### 버그 수정 (무음·자막 공백)
- **연속으로 잘린 단어 사이의 쉼("아 (0.5초) 아")이 오디오 조각으로 남던
  문제 수정** — 연속 컷을 그룹으로 묶어 사이 쉼까지 통째로 제거합니다.
- **숨소리·잡음이 자막 없는 오디오 공백으로 남던 문제 수정** — 무음제거본에서
  인식된 자막이 없는 유지 구간을 제거합니다.

### 실측 (동일 녹음 비교)
- 컷 33 → 57단어, 자막 사이 빈 공간 3 → 0개(검증 통과), 압축률 35% → 58%.
- 자동 커팅 회귀 테스트 17건 전부 통과.

## beta v0.12.7 — 2026-07-07 (군소리·다시 말하기 컷 대폭 개선)

### 버그 수정 (실녹음 검증 — 컷 3단어 → 33단어)
- **연속 군소리("아 아 어 음")가 통째로 남던 문제 수정** — 추임새가 연달아
  나오면 런(그룹) 단위로 판단해 전체를 잘라냅니다. 말 흐름에 붙은 추임새는
  여전히 보존합니다.
- **쉼을 두고 다시 읽은 반복("소녀는 (0.7초) 소녀는")이 남던 문제 수정** —
  강조/재읽기 구분을 시간이 아니라 종결어미로 판단합니다.
  ("감사합니다 (쉼) 감사합니다 여러분" 같은 강조 반복은 계속 보존)
- **재시작 앵커 감지 신설** — 쉼 뒤에 앞서 나온 문장 시작(2~3어절)이 그대로
  다시 나오면 그 사이의 미완성 시도를 통째로 잘라냅니다. "다시 읽을게요"
  같은 메타 발화도 함께 제거됩니다.
  - 오컷 방어: 완결 문장 포함 시 보존, 접속어미(~고/~며) 병렬 낭독 보존,
    이미 컷된 구간은 앵커 기준 금지 (연쇄 오컷 방지)
- 문어체 종결("펼쳤다/도착했다" 등 ~ㅆ다)을 완결 문장으로 인식합니다.

### 개선
- **화자 분리 목적 재정의: 한 타이틀 안에 두 사람의 목소리가 섞이지 않습니다**
  (Title Bible §7.3 신설) — 기존에는 자막을 만든 뒤 화자 라벨만 붙여 한 자막에
  두 화자가 섞일 수 있었지만, 이제 화자 경계가 자막 분리의 하드 경계입니다.
  짧은 맞장구("예", "네")도 다른 화자면 별도 타이틀이 되고, 화자별
  Video Role("스피커 N")이 부여됩니다. 1단어짜리 화자 오인(깜빡임)은
  양옆이 같은 화자면 흡수합니다.

### 문서·검증
- Title Bible §16 개정 (필러 런·재시작 앵커·종결어미 기준 + 설계상 한계 명시)
  및 §7.3 신설 (화자 경계 = 하드 경계).
- 자동 커팅 회귀 테스트 14건으로 확대 (실녹음 회귀 케이스 5건 추가),
  화자 턴 분할·깜빡임 스무딩 테스트 추가.

## beta v0.12.6 — 2026-07-07 (자동 커팅 기준 정식화 + 강조 반복 오컷 수정)

### 버그 수정
- **"감사합니다 (쉼) 감사합니다 여러분"처럼 문장을 맺고 강조로 반복한 발화를
  '다시 말하기'로 오인해 잘라내던 문제 수정** — 즉시 반복 컷은 이제
  ① 쉼 없이 바로 이어진 경우만, ② 앞쪽이 문장을 끝맺지 않은 경우만 적용됩니다.

### 문서·검증
- **Title Bible §16 신설** — 군소리 컷·다시 말하기 컷의 원리·원칙·예시 정식 수록.
  대원칙: "놓침은 괜찮지만 오컷은 안 된다". 담화표지(그/뭐/이제 등)는 의미
  용법과 구분할 수 없으므로 보존.
- 자동 커팅 회귀 테스트 신설 (§16 예문 9건 — 추임새/담화표지 보존/반복/재시작/경계).
- LLM 기반 커팅은 실측 결과(소형 모델 재현율 0%, 중형 모델 정밀도 20%)로
  배제 — 허용 조건을 §16.5에 명문화.

## beta v0.12.5 — 2026-07-07 (전체 옵션 활성화 + 문장 끝 단어 고아 금지 + 모델 관리)

### 새 기능
- **베타 집중 테스트 종료 — 모든 옵션 활성화**: 군소리 컷("어/음/아" 제거),
  다시 말하기 컷, 언어 선택(다국어/영어), 자막 형식(캡션/둘 다), 글자 크기,
  자막 길이, 빈틈없이 채우기, 화자 분리, 컷 강도를 자유롭게 조절할 수 있습니다.
- **AI 모델 관리** — 설정(⌘,) → AI 모델 탭의 모델별 카드에서
  `Finder에서 보기` / `삭제` / 개별 `다운로드`가 가능합니다.
  다운로드 진행률은 메인 창 배너에 표시됩니다.

### 개선 (Title Bible §4.1 확장 — 문장 끝 단어 고아 금지)
- **"…은혜의 시간이었다고 / 믿습니다"처럼 문장의 마지막 단어만 홀로 남던 문제 수정**
  — 직전 자막이 이미 최대 길이라 병합할 수 없으면 경계를 앞으로 당겨 재배분합니다:
  "기도의 끈으로 더 단단히 묶어주신 | 은혜의 시간이었다고 믿습니다".
- 규칙 분리기의 완화 경로·최종 폴백에서도 한두 글자짜리 조각과 금지 지점
  (부정 표현·접속부사 등) 절단이 불가능하도록 가드를 추가했습니다.
- 실제 37분 설교 자막 전체(46문장 → 123카드)로 전수 검증 — 고아 카드 0건,
  금지 지점 경계 0건. Bible 회귀 테스트 3건 추가(총 10건 통과).

## beta v0.12.4 — 2026-07-07 (AI 자막 분리 채택률 개선 + '수' 분리 금지)

### 개선
- **AI 자막 분리가 실제 발화에서 훨씬 자주 채택됩니다** — 실측 결과 언어모델이
  분리안을 내면서 단어를 살짝 바꾸는 경우("지금"→"현재" 등)가 많아 엄격 검증에서
  전부 탈락하고 있었습니다. 이제 **분리 위치만** 가져오고 자막 문구는 항상 STT
  원본 단어로 재구성하므로, 표기 이탈이 있어도 분리안을 안전하게 살립니다.
  (단어 추가·삭제·순서 변경은 여전히 거부 — 원문 무변경 보장은 그대로)
- **의존명사 '수' 앞 분리 금지** — "도망칠 | 수 없었다"처럼 부정 표현의 머리가
  갈라지던 지점을 Title Bible 금지 규칙에 추가했습니다.
- AI 분리가 문장별로 채택됐는지/왜 탈락했는지 로그에 남습니다
  (`[ai-split] 문장N 폴백: 원문 불일치…` 등) — 문제 제보 시 원인 파악이 쉬워집니다.

### 검증
- 60초 실영상 전 과정 추적(단어 타임코드→문장→분리)으로 확인, Bible 규칙
  테스트(§4.5/§4.6/§8.2 포함) 전부 통과. 모델 실험 결과 Qwen2.5-3B는 한국어
  표기 환각이 심해 1.5B 유지가 최선임을 확인.

## beta v0.12.3 — 2026-07-07 (AI 자막 분리)

### 새 기능
- **AI 자막 분리** — STT된 문장 전체의 문맥을 로컬 언어모델이 읽고, Title Bible 규칙
  위에서 분리 지점을 더 자연스럽게 다듬습니다. 목표는 "편집하지 않아도 될 자연스러움".
  - 타이밍 불변: AI는 분리 위치만 고르고 시간은 단어 타임스탬프 그대로
  - 엄격 검증: 원문 무변경·길이·Bible 금지 규칙을 통과한 문장만 채택,
    나머지는 기존 규칙 분리 유지 (품질 하한 보장 — 실패 불가능)
  - 언어모델(Qwen2.5-1.5B, 약 1GB)이 모델 다운로드에 추가됨 (최초 1회, 총 약 4.5GB)
  - 100% 로컬 처리 — 인터넷 없이 동작
- 진행바에 "문맥을 읽고 자막을 다듬는 중… 🪄" 단계 추가

## beta v0.12.2 — 2026-07-07 (진행바 개선 + 베타 카운터 리셋)

### 개선
- **진행바가 0→100%를 순차적으로 부드럽게 진행합니다** — 특정 구간에 멈췄다가 갑자기
  완료되던 구성을 앵커+보간 방식으로 재설계 (항상 전진, 점프 없음). 클립별 분석
  진행(13~70%)이 실제 작업과 연동되고, 요리 테마 단계 문구를 대폭 세분화했습니다.
  (기존에 분석 구간 진행률이 v0.12 로그와 어긋나 안 움직이던 문제도 함께 수정)
- **베타 사용 소진 카운터가 매 버전마다 리셋됩니다** — 업데이트하면 30회 재충전.

## beta v0.12.1 — 2026-07-07 (무음컷 중복 타이틀 수정)

### 버그 수정
- **무음컷으로 잘린 지점에서 같은 타이틀이 여러 번 반복되던 문제 수정** — 자막이
  발화 사이 침묵을 품은 채 컷에 걸치면 잘린 조각마다 같은 문장이 배치됐습니다.
  이제 자막은 **가장 많이 겹치는 조각 하나에만** 배치됩니다. 무음컷 후 타이틀
  위치·내용을 다시 손볼 필요가 없습니다.
- 회귀 테스트에 "경계 중복 타이틀" 자동 검사 추가 (소스 구간 겹침 기준 — 같은
  문장을 실제로 두 번 말한 경우는 정상 유지).

## beta v0.12.0 — 2026-07-07 (전면 구조 교체 — 메인 스토리라인 오디오 기준)

### 새 동작 원칙 (앱의 작동 방식 재정의)
- **분석 기준은 오직 메인 스토리라인(스파인 직속)의 오디오입니다.** 클립마다 소스가
  달라도 됩니다(다중 소스 OK). **커넥티드 클립의 오디오는 분석하지 않습니다.**
- **이해하지 못하는 요소는 오류 대신 통과합니다** — 멀티캠(mc-clip)·컴파운드(ref-clip)·
  b롤·gap이 있어도 작업이 멈추지 않고, 해당 요소는 위치를 보존한 채 그대로 지나갑니다.
- 무음컷에서 커넥티드·보조 스토리라인은 **트리밍 없이 부모 조각에 통짜 승계** —
  잘리지 않고 원위치에 보존됩니다.

### 왜 바꾸었나
타임라인의 모든 구성요소를 이해하려던 기존 구조가 반복 오류의 뿌리였습니다.
v0.12는 3계층(스캔→분석→산출)으로 재작성해 표면적을 근본적으로 줄였고,
macOS·파이널컷 버전 전방위(FCPXML 1.9~1.14)에서 동일하게 동작합니다.

### 검증
- 자동 회귀 13종(멀티캠 신규 포함) + Title Bible 7종 + 스캔 계층 13종
- FCPXML 1.9~1.14 버전 매트릭스 (버전 보존 + 각 DTD 통과)
- 실제 프로젝트(커피국수: 다중소스+어저스트먼트+b롤) 오브젝트 완전 보존
- 테스터 환경 재현(번들 런타임) 실오디오 E2E — 전부 통과

## beta v0.11.5 — 2026-07-06 (야간 자율 검증 3~7차)

### 개선
- **작업 취소 시 빨간 오류가 뜨던 문제 수정** — 실행 중 "초기화"를 누르면 이제 조용히
  준비 상태로 돌아갑니다 (이전엔 "작업이 실패했습니다 (종료 코드 15)" 오류 표시).
- 음성이 없는 클립(배경음악·효과음 가능)은 무음컷에서 **보존**하는 정책을 명문화하고
  자동 회귀에 고정 — 음악 클립이 실수로 삭제되는 일을 방지합니다.

### 야간 자율 검증 결과 (신규 결함 0)
- 자동 회귀 12종 확대: 음성 없는 클립·중첩 커넥티드·싱크 클립 추가
- 실제 프로젝트(커피국수) × 자막 옵션 전 조합, 60클립 대형, 다국어 혼합,
  번들 런타임(테스터 환경 재현) E2E — 전부 통과
- 베타 30회 카운트·업데이트 알림·옵션 전달 로직 점검 완료

## beta v0.11.4 — 2026-07-05 (야간 자율 검증 1·2차)

### 버그 수정
- **자막 "클립 경계 이탈" 경고 잔여 원인 수정** — 프레임 반올림이 자막을 클립 시작보다
  1프레임 미만 앞세우거나 끝을 넘기던 문제. 스냅 후 재클램프로 해결.
- 소스 매칭이 어긋난 클립(음수 타임코드)에 진단 경고 추가 — 원인 파악이 쉬워짐.

### 검증 확대 (자동 회귀 11종)
- 싱크 클립(sync-clip)·중첩 커넥티드 픽스처 추가 — 무음컷 시 내부 구조 유지 확인
- 자막본(결과물 1)에도 수치 정합 검사 적용 (기존엔 무음제거본만)
- "무음컷만 켬" 경로(자막 제거) 실제 E2E 검증 — DTD·정합 통과
- 업데이트 알림 버전 비교 로직 점검 (정상)

## beta v0.11.3 — 2026-07-05 (베타 집중 테스트 모드)

### 변경
- **베타 집중 테스트 모드** — 무음컷·자막 자동화 두 기능 검증에 집중하기 위해 세부
  옵션(언어/자막 형식/글자 크기/자막 길이/빈틈 채우기/화자 분리/컷 강도/군소리·다시
  말하기 컷)을 잠시 **비활성화**했습니다 (기본값으로 고정, UI에는 표시됨). 안정화되면
  다시 열립니다.
- 군소리 컷·다시 말하기 컷 기본값 끔 (집중 테스트 동안).

### 자막 엔진 (Title Generation Bible 반영)
- 자막 분리 규칙 명문화: [TITLE-GENERATION-BIBLE.md](TITLE-GENERATION-BIBLE.md)
- 접속부사("하지만/그리고" 등)가 자막 끝에 남지 않고 다음 자막 머리로 가도록 개선
- 부정 표현("~할 수 없었다", "~지 않았다")과 숫자+단위("열한 살") 분리 금지
- 한글 자막 텍스트 정규화(공백·반복 구두점) 단계 추가
- Bible 규칙 자동 테스트 7종 + 타임라인 회귀 9종 전부 통과

## beta v0.11.2 — 2026-07-05 (무음컷 정합 수정 + 권한 1회 설정)

### 버그 수정 (무음컷)
- **영상 위치 어긋남 수정** — 시작점(start) 정보가 없는 클립을 무음으로 분할하면 모든
  조각이 소스 처음부터 재생되던 문제. 조각마다 정확한 소스 위치를 기록합니다.
- **자막 중복 수정** — 프로젝트에 기존 타이틀이 있으면 무음제거본에서 기존 자막이 새
  자막과 겹쳐 복사되던 문제.
- 커넥티드 b롤의 앞부분이 잘릴 때 내용이 밀리던 문제 수정.
- 클립 끝에 걸린 0길이 자막으로 경계 이탈 경고가 나던 문제 수정.
- **보조 스토리라인(b롤 묶음)이 무음컷 시 통째로 사라지던 문제 수정** — 이제 위치를
  유지한 채 전부 보존됩니다.
- 무음컷 후 클립 사이 1프레임 빈틈/겹침(갭 유발) 문제 수정 — 스파인 경계를 프레임
  격자로 정규화해 어떤 프레임레이트·소스 조합에서도 이어붙습니다.

### 개선
- **디스크 접근 권한을 한 번만 설정하면 됩니다** — 베타 빌드 서명을 개선해 앱을
  업데이트해도 권한이 유지됩니다. (이번 v0.11.2 설치 시 한 번만 다시 켜주세요 —
  이후 업데이트부터는 유지됩니다)
- 전체 기능 재검증: 무음컷 수치 정합 검사(빈틈·겹침·자막 경계) 신설, 자막 형식
  3종×옵션 조합, FCPXML 버전 1.9~1.14 매트릭스(버전 보존+해당 DTD 통과), 타임라인
  구성 9종 회귀 — 전부 통과.

## beta v0.11.1 — 2026-07-05 (오래된 결과 파일 열림 문제 수정)

### 버그 수정
- **무음컷/자막 실행 후 이전 결과(원본으로 붕괴된 옛 파일)가 열리던 문제 수정.** 앱이 출력
  폴더의 모든 결과 파일을 훑다가, 이전 실행에서 남은 오래된 파일을 열던 것이 원인이었습니다.
  이제 **가장 최근에 생성된 결과물**만 선택합니다. ("원본 소스가 통째로 불려온다",
  "영상이 사라진다"는 증상의 주원인)
- 진단 로그에 **프로젝트 스파인 직속 클립 수**를 추가해 타임라인 구조 문제를 정확히 판별.

## beta v0.11.0 — 2026-07-05 (무음제거 재설계 — 오브젝트·컷 보존)

### 대변경 (근본 재설계)
- **무음제거가 이제 어떤 타임라인 오브젝트도 삭제하지 않습니다** — 기존에는 무음제거 시
  스파인을 통째로 다시 만들어 어저스트먼트 레이어·b롤·커넥티드 클립을 삭제/증식시켰습니다.
  이를 **비파괴 시간 리매핑**으로 재설계: 무음 구간만 타임라인 좌표로 수집해 전체를 압축하고,
  모든 오브젝트를 삭제 없이 이동·분할합니다. 어저스트먼트 레이어·b롤·gap·사용자 컷이
  전부 보존되고 침묵만 제거됩니다.
- **단일 소스 다중 컷(더빙) 프로젝트의 컷 보존** — 여러 컷으로 나눈 더빙 타임라인이
  하나로 붕괴되던 문제 해결.

### 견고화
- **버전별 DTD 자동 검증** — 결과물을 FCP 내장 DTD(입력 버전에 맞춰)로 생성 직후 검증,
  위반을 로그에 기록. 실제 커피국수(다중소스+어저스트먼트+b롤)로 오브젝트 보존 + FCP 1.13
  DTD 통과 검증 완료.
- **자동 회귀 테스트 스위트** — 프로젝트 유형별(단일/다중소스/커넥티드/어저스트먼트/b롤/gap)
  골든 테스트 신설(미디어 없이 실행). 오브젝트 보존·컷 보존·DTD를 자동 검증.
- 진단 로그에 **프로젝트 스파인 직속 클립 수** 추가 (컷 구조 문제 규명).

## beta v0.10.7 — 2026-07-05 (오브젝트 삭제 방지)

### 수정 (중요)
- **무음제거본이 어저스트먼트 레이어·b롤 등 타임라인 오브젝트를 삭제하던 문제** —
  무음 제거는 타임라인을 재구성하므로 오디오 클립 외 오브젝트(어저스트먼트 레이어,
  b롤 클립, 커넥티드 클립)를 지웁니다. "어떤 오브젝트도 삭제 금지" 원칙에 따라,
  이런 오브젝트가 있는 프로젝트는 **무음제거본을 생성하지 않고 자막본만** 만듭니다
  (아무것도 삭제하지 않음). 단순 오디오/비디오 클립만 있는 타임라인은 무음제거 정상 동작
- 무음제거 재구성의 클립 매칭 오류(중간에 무음 클립이 있으면 이후 클립이 어긋나던
  zip 오정렬) 수정 — 미지원 대상은 위 규칙으로 아예 생성 안 함

## beta v0.10.6 — 2026-07-05 (어저스트먼트 레이어 증식 수정)

### 수정 (중요)
- **무음제거본에서 어저스트먼트 레이어(및 커넥티드 클립)가 여러 개로 복제되던 버그** —
  무음 제거 시 클립을 음성 구간별로 쪼개는데, 클립에 붙은 lane 위의 어저스트먼트 레이어를
  매 조각마다 복사해 레이어가 N배로 증식하던 문제(import 경고·타임라인 혼란 유발).
  이제 재구성 시 커넥티드/어저스트먼트 레이어를 복제하지 않음
  (결과물1 "공백메움"에는 어저스트먼트 레이어가 그대로 1개 유지됨)

## beta v0.10.5 — 2026-07-05 (무음제거본 import 수정)

### 수정 (중요)
- **무음제거본 import 시 "Invalid edit with no respective media" 경고 + 타임라인 갭** —
  무음 제거 재구성에서 만든 클립이 원본 에셋의 미디어 범위를 벗어나거나 offset이
  축약분수로 표현되던 문제. 이제 (1) 클립 범위를 미디어 가용 범위로 클램핑,
  (2) offset/start/duration을 프레임 타임베이스로 표현, (3) 미디어를 해석 못한 클립은
  통째로 유지(잘못된 오디오로 자르지 않음). 다중 소스 무음제거본이 깨끗하게 import됨

## beta v0.10.4 — 2026-07-05

### 수정
- 언어 옵션 순서를 **자동 / 한국어 / English** 로 정리
- 자동 모드의 **?** 아이콘 — 마우스 호버 시 툴팁이 안 뜨던 문제 수정.
  이제 클릭하면 지원 언어 목록이 팝오버로 표시됨 (호버 툴팁도 병행)

## beta v0.10.3 — 2026-07-05 (무음컷 다중소스 수정)

### 수정 (중요)
- **무음컷을 켜면 필요한 소스까지 잘리던 버그** — 여러 소스로 편집된 프로젝트에서
  무음 제거가 첫 번째 소스의 오디오만으로 음성을 감지해, 다른 소스의 클립을 전부
  무음으로 판단하고 잘라내던 문제(커피국수: 10.5분 → 4.5초, 99% 컷).
  이제 무음제거도 **클립별 자기 소스 오디오로 음성을 감지**합니다 (자막 경로와 동일)

## beta v0.10.2 — 2026-07-05

### 새 기능
- **자동 업데이트 안내** — 구버전 사용 시 상단에 새 버전 배너 표시.
  "수정된 내용 보기"로 릴리스 노트(변경 내역)를 앱 안에서 확인하고 바로 업데이트
- **상세 진단 로그** — 작업 완료 요약에 클립 통계(전체/음성있음/건너뜀), 자막 수,
  자막 폰트, 언어 모드, 자동 컷 내역, 적용 옵션을 기록 → 버그 리포트 시 원인 파악 향상

## beta v0.10.1 — 2026-07-05

### 개선
- 언어 옵션 옆 **? 아이콘** 추가 — 마우스를 대면 "자동" 모드가 감지하는 지원 언어
  목록(한/영/일/중/광둥/독/프/스/이/포/러)이 표시됨

## beta v0.10.0 — 2026-07-05 (UI 개편 + AutoSubs 참고 기능)

### UI/UX
- **상단 헤더 + 상태 표시** — 앱 타이틀·버전, 모델/입력 준비 상태 색상 dot
- **우측 상단 ⋯ 메뉴** — 피드백·로그 다운로드·다운로드 폴더·최신 버전 확인을 한곳에 모음
  (하단 버튼 영역 정리 → 실행 버튼 강조)
- **앱 메뉴바 + 설정 화면(⌘,)** — 도움말 메뉴, 환경설정에 고유명사·모델 관리 이동
- **로그 색상 코딩** — 정보/성공/경고/오류를 색으로 구분

### 새 기능 (AutoSubs 참고)
- **화자 분리 (색상 구분)** — 화자별로 FCP Video Role "스피커 N" 부여.
  라이브러리에 "스피커 1/2/3" 롤을 색상과 함께 만들어두면 자막이 화자별 색으로 표시
- **커스텀 프롬프트** — 설정에서 고유명사·용어(사람 이름, 교회명 등)를 입력하면
  ASR 인식 정확도 향상 (Qwen system_prompt 활용)

## beta v0.9.3 — 2026-07-05 (import 수정 + 다국어)

### 수정 (긴급)
- **"편집 프레임 경계 이탈" import 거부 (FCP 12.3)** — 자막 offset이 축약분수(예 11/10)로
  표현돼 FCP가 프레임 경계로 인정하지 않던 문제. 프레임 타임베이스(예 3300/3000)로 수정
- **한/영 혼합 영상에서 영어가 한국어로 오인식** — 언어 "자동 (다국어)" 옵션 추가.
  선택 시 구간별로 언어를 감지해 한국어/영어를 정확히 분리

### 개선 (AutoSubs 참고)
- **한글 자동 폰트 폴백** — 한국어/다국어 자막에 Apple SD Gothic Neo 자동 지정
  (Helvetica는 한글 글리프가 없어 깨짐). 다국어 모드는 한글+라틴 모두 렌더
- **ffmpeg 견고성** — 번들→시스템 ffmpeg 폴백 + 다운믹스 실패 시 pan 필터 재시도

## beta v0.9.2 — 2026-07-05 (문장 경계 자막 분리)

### 개선
- **문장 경계 우선 자막 분리** — 자막을 나누기 전에 먼저 문장 단위로 분리한 뒤
  각 문장 안에서만 길이 분할. 한 자막에 "…합니다"(문장 끝)와 "저는…"(다음 문장 시작)이
  섞이는 일이 구조적으로 불가능해짐.
  예) "안녕하세요 저는 김한별이라고 합니다 저는 서울에서 살고 있어요"
  → "안녕하세요 저는 김한별이라고 합니다" / "저는 서울에서 살고 있어요"
- 문장 내 의미 단위 분할(어절 보존)은 그대로 유지

## beta v0.9.1 — 2026-07-05 (커넥티드 클립 지원)

### 새 기능
- **커넥티드 클립(연결된 클립) 음성 인식** — 이제 메인 스토리라인뿐 아니라
  스파인 위/아래 레인에 붙은 커넥티드 클립(별도 인터뷰 오디오, 인서트 등)의 음성도
  추출·인식해 자막을 생성. 각 클립은 자기 소스 파일에서 오디오를 뽑고
  타임라인 시간 위치에 맞춰 배치됨
- 참고: 무음제거본(결과물 2)은 메인 스토리라인 기준으로 재구성 (커넥티드 클립 자막은
  자막본(결과물 1)에 반영)

## beta v0.9.0 — 2026-07-05 (다중 소스 지원 + 전체 기능 정상화)

### 수정 ("계셨" 자막 버그 최종 해결 — tinfish님 로그로 확정)
- **다중 소스 편집 프로젝트 지원** — 여러 다른 영상 파일로 편집된 프로젝트에서
  파이프라인이 첫 번째 소스 하나만(예: 16초짜리) 오디오로 추출해 나머지 클립을
  전부 "음성 없음"으로 건너뛰던 치명적 버그. 이제 **클립마다 자기 asset의 파일에서**
  오디오를 추출 (파일당 캐시). 33개 클립 중 7개만 인식되던 문제 해결
- 자막이 클립 경계를 살짝 넘던 문제 — 클립 duration 기준 재클램핑으로 완전 해결

### 변경
- **모든 기능 재활성화** — 무음컷·군소리 컷·다시 말하기 컷·자막 자동화 전체 정상 복귀
  (v0.8.x 격리 테스트 종료). v0.8.3의 환경 완전 일치 런타임 기반

## beta v0.9.0 — 2026-07-05 (전체 기능 정상화)

### 변경
- **모든 기능 재활성화** — v0.8.x의 자막-자동화-단독 격리 테스트를 종료하고
  무음컷·군소리 컷·다시 말하기 컷·자막 자동화 전체를 정상 복귀.
  v0.8.3의 환경 완전 일치 런타임(Python 3.11 + 검증된 148개 패키지) 기반이라
  soynlp 누락 등 환경 차이 버그가 해결된 상태에서 전체 기능 제공
- 유지: 자막 형식/글자 크기/자막 길이/빈틈없이 채우기, 화자 색상(코드),
  드래그 입출력, 피드백, 로그 다운로드(상시), 진행 표시, 모델 자동 감지

## beta v0.8.3 — 2026-07-04 (환경 완전 일치)

### 변경
- **번들 런타임을 개발 환경과 완전히 동일하게 재구성** — Python 3.11 + 검증된 venv의
  전체 패키지(148개)를 동일 버전으로 미러링 (torch 기반 VAD 포함, soynlp 포함).
  수작업 최소 목록에서 생기던 환경 차이 버그(soynlp 누락 등)를 원천 차단.
  용량은 커지지만 정확성 우선 (macOS 14 호환 mlx 휠 교체는 유지)

## beta v0.8.2 — 2026-07-04

### 개선
- **로그 다운로드를 항상 사용 가능하게** — 성공·실패·진행 중·취소 후 상관없이,
  로그가 있으면 하단에 [로그 다운로드] 버튼이 항상 표시됨

## beta v0.8.1 — 2026-07-04 (중요 수정)

### 수정 ("계셨" 자막 버그 — tinfish님 로그로 원인 확정)
- **번들 런타임에 soynlp 누락** — 자막 타이밍 정렬기의 한국어 토큰화 의존성이 빠져
  테스터 Mac에서 정렬이 실패하고 조각 자막("계셨.")이 생기던 문제. 런타임에 추가
- **오디오 진단 로그 추가** — 추출된 오디오의 길이·샘플레이트·볼륨(RMS)을 로그에 기록.
  ffmpeg가 특정 카메라(소니 C0022 등) 오디오를 제대로 못 뽑는 정황이 있어
  다음 로그로 확정 진단 예정

## beta v0.8.0 — 2026-07-04 (단순화 테스트 버전)

### 변경
- **자막 자동화만 활성화한 격리 테스트 버전** — 반복되는 오류의 원인을 좁히기 위해
  무음컷·군소리 컷·다시 말하기 컷을 일시적으로 숨김 (코드는 유지, 안정화되면 하나씩 재활성)
- 유지되는 것: 자막 형식/글자 크기/자막 길이/빈틈없이 채우기, 드래그 입출력,
  피드백 버튼, 로그 다운로드(성공·실패 모두), 진행 표시, 모델 다운로드
- 결과물: 자막본 1개만 생성 (무음제거본 없음)

## beta v0.7.10 — 2026-07-04

### 수정
- **컴파운드 클립 안에 자막이 생성되고 같은 자막이 반복되던 버그** — 스파인 탐색이
  문서의 첫 번째 spine(컴파운드 내부 시퀀스)을 잡던 문제. 항상 프로젝트의 스파인만
  처리하도록 수정. 컴파운드 프로젝트는 명확한 안내(Break Apart) 표시

## beta v0.7.9 — 2026-07-04

### 수정
- **"항목이 편집 프레임 경계에 있지 않음" import 경고 수정** — 카메라 TC가 프레임
  격자에 정렬되지 않은 소스에서 자막 위치가 격자를 벗어나던 문제. 자막 offset을
  클립 기준 상대 프레임 배수로 계산하도록 변경 (크므츠님 사례)
- **성공 로그에 결과물 fcpxml 구조 포함** — 클립별 자막 개수·위치 샘플까지 기록되어
  자막 품질 문제를 로그만으로 진단 가능
- 지원하지 않는 프로젝트 구조일 때 스파인 구성(태그별 개수)을 오류에 표시,
  컴파운드(ref-clip)는 Break Apart 임시 방법 안내 (June님 사례)

## beta v0.7.8 — 2026-07-04

### 개선
- **완료 화면에도 "로그 다운로드" 버튼** — 작업이 성공해도 로그를 저장할 수 있음.
  자막이 이상하게 나오는 등 결과 품질 문제를 리포트할 때 사용
  (검증 통계·자동 커팅 내역·환경 정보가 모두 담김)

## beta v0.7.7 — 2026-07-04

### 개선 (진단 로그 강화)
- 로그 다운로드 파일에 버그 픽스용 환경 정보 추가:
  칩 종류(Apple Silicon/Intel), 메모리·디스크 여유, **Final Cut Pro 버전**,
  파이프라인 경로, AI 모델 캐시 상태(모델별 위치), 입력 XML 구조(버전·클립 타입별 개수),
  **미디어 파일 실존 여부**(외장/NAS 미연결 감지)

## beta v0.7.6 — 2026-07-04

### 변경
- **화자 분리 제거** — 화자별 텍스트 색상이 FCP 타이틀에 반영되지 않는 문제로
  기능을 제거하고 앱을 단순화 (처리 속도 향상). CLI에는 `--no-diarize` 플래그 추가
- 번들 완전성 감사 통과 — macOS 15 호환 검증(전용 휠·미지원 바이너리 0개)

## beta v0.7.5 — 2026-07-04

### 수정
- **FCP import 실패("DTD 확인 실패 — asset-clip") 수정** — 오디오 채널 설정이 있는
  클립(audio-channel-source)에서 자막이 잘못된 위치에 삽입되어 import가 거부되던 문제.
  FCPXML 규격 순서에 audio-channel-source를 반영해 올바른 위치에 삽입

## beta v0.7.4 — 2026-07-04

### 개선
- **100% 순수 Apple Silicon** — 번들 내 모든 실행 파일·라이브러리에서 x86_64 슬라이스 제거
  (유니버설 바이너리 2개를 arm64 전용으로 변환). Intel 전용 바이너리 0개 검증
- 빌드 스크립트에 자동 보증 추가 — Intel 전용 바이너리가 섞이면 빌드 자체가 실패하도록
  하여 "Intel 앱" 경고 재발을 원천 차단

## beta v0.7.3 — 2026-07-04

### 수정
- **"Intel 기반 앱 지원 종료" 경고 제거** — 번들된 정적 ffmpeg가 Intel(x86_64) 빌드여서
  macOS가 Intel 구성 요소 경고를 띄우던 문제. arm64 정적 빌드(martin-riedl.de)로 교체,
  이제 앱 전체가 순수 Apple Silicon 바이너리

## beta v0.7.2 — 2026-07-04

### 수정
- **베타에서 "키체인 암호" 창이 뜨던 문제** — 사용 카운터의 Keychain 저장이 베타 빌드에도
  적용되어 있었음. 베타는 버전마다 서명이 바뀌어 업데이트 시마다 암호를 묻게 되므로
  베타 빌드는 Keychain을 사용하지 않도록 수정 (카운터는 UserDefaults 저장, 30회 유지).
  이미 창이 뜬 테스터는 "거부"를 눌러도 앱 사용에 지장 없음

## beta v0.7.1 — 2026-07-04

### 개선
- **모델 재다운로드 방지** — 시스템 HuggingFace 캐시(~/.cache/huggingface)에 모델이
  이미 있으면 그대로 재사용. 앱 업데이트·재설치 후에도 모델을 다시 받지 않음
  (모델 저장 위치: 앱 전용 폴더 또는 기존 시스템 캐시 중 있는 곳)
- **빈 결과물 방지** — 유지된 음성 구간이 없으면 빈 무음제거본을 저장하는 대신
  "컷 강도를 낮춰보세요" 경고 표시 (커피국수 사례 재발 방지)

## beta v0.7 — 2026-07-04

### 새 기능
- **싱크로나이즈드 클립(sync-clip) 지원** — 스파인의 sync-clip을 내부 asset-clip 기준으로
  해석해 음성 인식·무음컷·자막이 모두 동작. 실제 프로젝트로 E2E + FCP DTD 검증 완료
- **화자별 타이틀 색상** — 화자 분리 시 화자마다 자막 색이 다르게 표시
  (화자1 흰색 / 화자2 노랑 / 화자3 하늘 / 화자4 연두, FCP Role 구분과 병행)

### 수정
- 자막 요소가 sync-source·marker보다 뒤에 삽입되어 DTD 검증에 실패하던 문제 —
  FCPXML 규격 순서대로 올바른 위치에 삽입
- 에셋에 시작 TC가 없을 때(start 속성 없음) 타입 오류로 실패하던 문제

### 예정
- 멀티캠(mc-clip)·컴파운드(ref-clip) 지원 — 샘플 수급 후 구현

## beta v0.6.1 — 2026-07-04 (긴급 수정)

### 수정
- **macOS 14/15에서 AI 엔진이 시작되지 않던 치명적 버그** —
  "Failed to load the default metallib (language version 4.0)" 오류.
  빌드 Mac(macOS 26)에서 mlx가 macOS 26 전용 휠로 설치되던 문제로,
  macOS 14 호환 휠(Metal 3)로 교체. **macOS 15 테스터는 반드시 이 버전으로 업데이트 필요**

### 예정 (v0.7)
- 멀티캠(mc-clip) · 컴파운드(ref-clip) · 싱크로나이즈드(sync-clip) 클립 지원 —
  메인 스토리라인 기준 처리

## beta v0.6 — 2026-07-04

### 개선
- **모델 다운로드 진행 표시** — 다운로드 중인 모델 이름("Qwen3 음성 인식 모델" 등),
  진행률 바 + %, 다운로드 속도(MB/s), 몇 번째 모델인지(1/2) 표시.
  실제 받은 바이트 기준이라 네트워크가 느려도 정확하게 반영됨
- **멀티캠 프로젝트 안내** — 멀티캠(mc-clip) 프로젝트를 넣으면 명확한 안내와 임시 해결책
  (Break Apart Clip Items) 표시. 멀티캠 정식 지원은 v0.7 예정

## beta v0.5 — 2026-07-04

### 새 기능
- **자막 빈틈없이 채우기** 옵션 (기본 켬) — 끄면 실제 말하는 구간에 맞게
  자막 길이 조정 (짧은 끊김 메움 없이 순수 발화 타이밍, `--no-gap-fill`)
- **실패 시 "로그 다운로드" 버튼** — 앱 버전·macOS·입력 파일·선택 옵션이 포함된
  전체 로그를 다운로드 폴더에 저장 (피드백 첨부용)

### 베타 정책
- 베타 사용 횟수 **30회 고정** (소진 시 "정식 출시를 기다려주세요" 안내, 구매 버튼 없음)
- 개발 빌드는 무제한 유지, 정식 빌드는 무료 데모 10회 + PRO 구매

## beta v0.4 — 2026-07-04

### 새 기능 (자동 커팅 — Auphonic Automatic Cutting 참고, 한국어 지원)
- **군소리 컷**: 홀로 발화된 추임새("어/음/아/흠" 등)와 기침·헛기침(음성 감지됐지만
  단어가 없는 짧은 구간)을 무음제거본 타임라인에서 잘라냄. 단어 정렬 타임스탬프 기반이라
  문장 속 진짜 단어("아 그렇군요"의 "아")는 건드리지 않음 — 앞뒤로 띄어진 단독 발화만 컷
- **다시 말하기 컷**: (1) 즉시 반복("우리가, 우리가") (2) 문장을 끝맺지 못하고 비슷한
  문장으로 다시 시작한 경우 미완성 쪽을 잘라내고 완성된 문장만 남김.
  잘린 단어는 자막에서도 함께 제외됨
- 두 기능 모두 무음컷 아래 체크박스 (기본 켬), CLI `--cut-fillers` / `--cut-retakes`

### 호환성
- **FCPXML 버전 호환 (FCP 10.x ~ 12.3)**: 구버전 추출본(DTD 1.8 미만)은 자동으로 1.8로
  승격해 caption 요소가 DTD 검증을 통과하도록 함. 아주 오래된 `<clip>` 구조(2018 이전)는
  친절한 안내 메시지와 함께 재내보내기 유도

### 용량 (베타 DMG 43% 절감)
- VAD의 torch 의존 제거 — silero ONNX 모델(2.2MB)을 내장하고 onnxruntime으로 실행.
  torch·torchaudio·sympy 등 약 400MB 제거, 번들 런타임 950MB → 543MB.
  개발 venv는 기존 torch 경로 유지 (결과 차이 ≤ 0.02초 검증 완료)

### UI/UX
- 진행 문구를 **순차 표시 큐**로 변경 — 진행률(%)은 실시간으로 움직이되,
  문구는 각각 1.8초씩 읽을 수 있게 하나씩 표시 (밀리면 오래된 문구부터 생략)

## beta v0.3 — 2026-07-04

### 변경
- **AI 문맥 교정 제거** — 앱을 단순하게 유지. 자막 품질의 핵심은 의미 단위 분할로 집중
  (silence-cutter의 `--polish` CLI 플래그는 남아 있으나 앱에서는 사용 안 함)
- **피드백 버튼** — 클릭 시 Fillout 피드백 폼(forms.fillout.com/t/1Hnsen7DLZus)을 브라우저에서 열도록 연결
  (기존 메일 초안 방식 및 FeedbackView 제거)

### 개선 (의미 단위 분할)
- 존칭 관형형(하신/되신/주신/가신/오신/계신)을 분할 경계로 인식 —
  "…걷기 하신 | 하나님께 영광 돌립니다"처럼 길이 초과 시 자연스러운 지점에서 나뉨
- "짧게" 프리셋에서 4~5자 단편 자막이 생기던 문제 수정 —
  남은 꼬리가 너무 짧으면 자르지 않고 이어붙임 (약간의 길이 초과 허용)
- 세 가지 길이 프리셋(18/27/38자) 전부에서 의미 단위 보존 검증 완료

## beta v0.2 — 2026-07-04

### 새 기능
- **글자 크기** 옵션: 작게(32pt) / 보통(42pt) / 크게(54pt) / 아주 크게(68pt) — 타이틀 자막 폰트
- **자막 길이** 옵션: 짧게(한 줄 18자) / 보통(27자) / 길게(38자) — VREW의 글자 수 지정 방식 참고,
  길이와 무관하게 분할은 항상 문맥(의미 단위) 우선
- **AI 문맥 교정** 토글: 로컬 LLM(Qwen2.5-3B)이 오인식·구어체를 문맥에 맞게 다듬음.
  타이밍·의미 불변, 처리 시간 증가. (silence-cutter에 `--polish` 신설, 교정 3순위로 통합)

### UI/UX
- 실행 중 화면을 로그 대신 **프로그레스 바 + doodling 스타일 한글 문구**로 교체
  ("소리 안 들리는 구간을 사시미로 써는 중… 🔪", "자막을 노릇노릇 굽는 중… 🔥" 등)
- 음성 인식 구간은 실제 진행률(i/total)로 바가 움직임
- 원본 로그는 "자세한 로그" 접이식으로 이동, 실패 시에만 자동 표시

### 수정
- 개발 빌드에서 리빌드할 때마다 키체인 암호를 묻던 문제 수정 — 사용 카운터의 Keychain 저장은
  서명이 고정된 앱스토어 정식 빌드에서만 사용 (개발/베타는 UserDefaults)
- 데모 10회 제한을 앱스토어 정식 빌드 전용으로 변경 — 개발·베타 빌드는 카운터 없이 무제한

## beta v0.1 — 2026-07-04

첫 베타 버전.

### 기능
- 파이널컷 프로젝트 드래그앤드롭 입력 (FCP에서 직접 드래그 또는 .fcpxmld/.fcpxml 파일)
- 무음컷: Silero VAD 기반 무음 구간 제거, 컷 강도 4단계 (약하게/보통/강하게/매우 강하게)
- 자막 자동화: Qwen3-ASR(1.7B) + ForcedAligner 음성 인식, 의미 단위 우선 자막 분리
- 자막 형식 선택: 타이틀(기본) / 캡션 / 둘 다
- 결과물을 파이널컷으로 바로 드래그해서 import (FCPXML 페이스트보드 지원)
- AI 모델 자동 감지 + 미설치 시 앱 내 다운로드 (약 3.5GB)

### 베타
- 베타 빌드는 사용 제한 없음 (데모 10회 제한은 앱스토어 정식 빌드 전용)
- 앱 내 "피드백" 버튼 — 앱 버전/macOS 정보 자동 첨부, 메일로 전송 (webhook 연동 가능)
- 베타 배포 DMG 패키징: 번들 Python 런타임 + 정적 ffmpeg 포함 (다른 Mac에서 자기완결 실행)

### 유료화 (정식 빌드)
- 무료 데모 10회 (성공한 프로젝트 추출 기준, 재설치해도 유지)
- 소진 후 PRO 영구버전 구매 (StoreKit 2, App Store Connect 등록 예정)

### 빌드
- `./build_app.sh` 개발용 / `appstore` MAS 구조 / `dmg` 베타 배포 DMG
- `./build_runtime.sh` 자기완결 런타임 생성 (~865MB)

### 알려진 제한 (앱스토어 출시 전 해결 예정)
- 앱스토어 빌드에 ffmpeg 미포함 — AVFoundation 오디오 추출로 대체 예정
- 샌드박스 미디어 폴더 접근 권한 UX 미구현
- 앱 아이콘 없음
